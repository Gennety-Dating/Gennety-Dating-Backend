# Test environment — used by integration / E2E tests only.
# Matches the credentials in docker-compose.test.yml.

DATABASE_URL=postgresql://gennety:gennety@localhost:5433/gennety_test

BOT_TOKEN=test:integration-token
OPENAI_API_KEY=sk-test
SMTP_HOST=localhost
SMTP_PORT=587
SMTP_USER=test
SMTP_PASS=test
SMTP_FROM=test@test.invalid
ADMIN_API_KEY=test-admin-key
ADMIN_PORT=3100
ADMIN_DASHBOARD_ORIGIN=*
WEBAPP_URL=https://test.invalid/calendar

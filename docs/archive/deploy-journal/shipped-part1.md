<!-- WHEN_TO_READ: HISTORICAL. Only when you need the verification steps or rollback of an ALREADY SHIPPED deploy (entries 2026-08-21 .. 2026-08-11). Find it via INDEX.md first — never read this file whole. -->
<!-- SOURCE: deploy.md (lines 3262-6177) — migrated 2026-09-01 -->

# Shipped deploy journal — part 1 of 3

Index of every entry: [INDEX.md](../../operations/deploy-journal/INDEX.md). Order is preserved from the original file (newest first).

---

**Deployed 2026-08-21 — релиз `57cb108`: возврат за недоставленный реметч +
приём клиентской воронки (`client_events`).** Полный деплой кода + аддитивный
`db:push`. **Mini App НЕ передеплоивался** — в диапазоне `c577665..57cb108` нет
ни одного файла под `apps/webapp` (две новые i18n-строки живут в
`@gennety/shared`, от которого вебапп намеренно не зависит). Демо задеплоено.

Порядок: worktree-снимок → preflight → `migrate diff` → rsync → install/build →
`db:push` → `db:drift-check` → `pm2 restart` → `demo:deploy`.

**Схема:** ровно один `CREATE TABLE client_events` + 3 индекса + 1 FK, **ноль
`DROP`** — план прочитан до запуска. `db:drift-check` **OK** ДО рестарта, что
важно именно здесь: ночной свип ретеншена читает новую таблицу, то есть база без
неё бросила бы `P2022` на первом же тике, а не только на первом батче.

**Деплоено из изолированного worktree**, потому что в дереве параллельно шла
чужая сессия (два docs-коммита Voice Prompts, `c304d71` + `1bd8e40`, приехали
попутно). Оставленный worktree'ем файл-указатель `/opt/gennety/.git` удалён
вручную — документированный `--exclude '.git/'` ловит только каталоги.

**Проверено на живом проде (замерено, а не выведено):**

- Прод **байт-в-байт равен `57cb108`** по всем 818 файлам (md5-свип, ноль
  расхождений — включая `apps/bot/tmp/`, которого в этом релизе не оказалось).
- Все 18 кронов + peer-wait воркер, `:3100`/`:3101` слушают, рестарт 64 → **65**,
  `unstable restarts: 0`, **в error-лог с момента рестарта не записано ни строки**
  (последняя запись 08:00 при рестарте в 08:27).
- `POST /v1/client/events` → **404**, то есть маршрут смонтирован и корректно
  инертен: `CLIENT_EVENTS_ENABLED` в прод-`.env` НЕ добавлялась, и добавлять её
  этим релизом нельзя (privacy manifest приложения пока заявляет, что аналитику
  мы не собираем). `client_events` существует и пуста.
- `rematch_purchases` пуста — покупок за всю историю ноль, так что рельса
  возврата уехала живой и ни разу не исполненной. Проверять на `@gennetytestbot`.
- `strandedProposed: 0` держится; строк `pitch reached nobody` / `undelivered=` в
  логе нет.
- Демо: схема применена к `aws-1-eu-west-1` (у прода `aws-0`), баннер изоляции
  называет `@gennety_demo_bot`, оба демо-подавления кронов на месте, рестарт
  28 → 29, `demo-api` ping ok, `demo-app/onboarding.html` 200. **Продовый бот при
  этом не перезапускался** — счётчик держится на 65.
- Ловушка с demo-указывающим `dist/` проверена по СОДЕРЖИМОМУ, а не по памяти:
  0 файлов с `demo-api`, 4 с `dating-api`.

rsync dry-run дал **273** удаления, все прочитаны и все под `apps/video/{build,out}`
(артефакты Remotion + `.DS_Store`); вне этого каталога — ноль. Оба ключа
`keys/*.p8`, оба бэкапа БД и все 18 снапшотов `.env.bak.*` проверены на месте
ПОСЛЕ синка.

**Rollback:** пересинкать чекаут на `c577665`, перезапустить, оттуда же
редеплоить демо. Аддитивная таблица может остаться — старый код её не читает.

---

**Deployed 2026-08-21 — реметч, чей питч не дошёл ни до кого, возвращает звёзды
(PRODUCT_SPEC §3.11, REMATCH_PRODUCT_SPEC шаг g, DECISIONS.md).** **Нет изменения
схемы Prisma, нет новых env, нет флагов, нет изменения Mini App** (`apps/webapp`
не тронут — он намеренно не зависит от `@gennety/shared`, поэтому две новые
i18n-строки его не касаются). Только бот: полный деплой кода несёт всё целиком,
плюс `pnpm demo:deploy`.

Продолжение вчерашней правки: `disposeUndeliveredMatch` освободил слоты и
разрешил купить снова, но деньги оставались потраченными. Теперь исход
«питч не увидел никто» — третий возвратный класс, `refunded_undelivered`.

**Шесть вещей, которые стоит знать до рестарта:**

- **`REMATCH_FEATURE_ENABLED=true` в проде, но покупок за всю историю НОЛЬ**
  (`rematch_purchases` пуста), так что первое исполнение будет первым в принципе.
  Проверять на `@gennetytestbot`, а не ждать сигнала из прода.
- **Появляется новая причина возврата в фаундерской ленте.**
  `notifyFounderPurchaseRefunded` шлёт `reason: "refunded_undelivered"` —
  строка, которой раньше не существовало. Её появление означает не «пользователь
  передумал», а «мы не смогли доставить анкету»; это ops-сигнал, а не оборот.
- **Новый статус — обычная строка, поэтому миграции нет.** `status` в
  `rematch_purchases` не Prisma-enum (ARCHITECTURE), так что четвёртое значение
  стоит ноль. Читатели, фильтрующие по списку возвратных статусов, обязаны
  добавить его — в коде такой список ровно один (`REFUNDED_STATUSES`).
- **Недельный лимит освобождается сам.** Он считает строки `settled`; уход с
  этого статуса возвращает попытку. Отдельной правки лимитов не потребовалось,
  и это то, что делает копию «попытка не засчиталась» правдой.
- **Две новые строки в логе, обе — грепаемые.**
  `[rematch] pitch reached nobody match=… purchase=… — refunding` (ERROR) и
  `undelivered=N` в итоговой строке `[dispatch] done:` (печатается только когда
  не ноль). Пусто = здорово.
- **Что НЕ возвращается, и это осознанно:** пожизненный бан пары. Кандидатка
  сгорает для покупателя даже неувиденной, потому что бан пишется при СОЗДАНИИ
  матча; снимать его значило бы удалить строку матча, чего продукт не делает
  нигде. Именно эта необратимая потеря и есть довод за возврат денег.

Preflight по этому изменению: typecheck чист по 5 проектам, lint чист, **полный
набор бота 3811 тестов / 247 файлов, 0 failed** (+6 новых). Обе положительные
проверки подтверждены **красными** по отдельности: занейтраленная ветка возврата
роняет 2 теста в `payments-rematch.test.ts`, а снятая отчётность `undelivered` —
1 тест в `dispatch-queue.test.ts`. Три отрицательных теста (дошло до одной
стороны / очередь бросила / обычная доставка) намеренно остаются зелёными в обоих
случаях — они проверяют ОТСУТСТВИЕ возврата.

Проверка после деплоя — счастливый путь ничего не логирует, так что смотрим
отсутствие строк, а сквозная проверка делается на dev-боте (нужен мужчина без
живого матча, `REMATCH_FEATURE_ENABLED=true`, и партнёрша, заблокировавшая бота):

```sh
pm2 logs gennety-bot --lines 200 --nostream | grep -E "pitch reached nobody|undelivered="
# Пусто = ни одна рассылка не провалилась полностью.
psql "$DATABASE_URL" -c "select status, count(*) from rematch_purchases group by 1;"
# refunded_undelivered > 0 = рельса сработала; смотреть тогда на достижимость
# партнёрш (403, mobile-стороны), а не на эту правку.
```

**Rollback:** откатить код и перезапустить. Откатывать больше нечего — ни схемы,
ни env, ни флага. Уже сделанный возврат остаётся сделанным, и это правильное
состояние: деньги вернулись, попытка не засчитана.

---

**Deployed 2026-08-21 (was PENDING) — приём клиентской воронки нативного приложения
(`POST /v1/client/events`, iOS 6.2; PRODUCT_SPEC §GDPR, ARCHITECTURE →
`client_events`, DECISIONS.md ×4).** **Нет изменения Mini App**
(`apps/webapp` не тронут) — но нужен **аддитивный `db:push` ДО рестарта** и
**одна новая env, которая уезжает ВЫКЛЮЧЕННОЙ**. Порядок: Deploy Full Server
Code → `db:push` → `pnpm db:drift-check` → `pm2 restart` → `pnpm demo:deploy`.

Новая таблица `client_events` читается и пишется маршрутом и **свипом
ретеншена**, который идёт по крону каждую ночь, — то есть база без неё бросит
`P2022` на первом же ночном тике, а не только на первом батче. Сначала
убедиться, что план аддитивный (ожидается один `CREATE TABLE`, три
`CREATE INDEX`, один FK, **ноль `DROP`**):

```sh
export DATABASE_URL="$(sed -n 's/^DATABASE_URL=//p' .env | tail -1 | tr -d '"')"
pnpm --filter @gennety/db exec prisma migrate diff \
  --from-schema-datasource prisma/schema.prisma \
  --to-schema-datamodel prisma/schema.prisma --script
pnpm --filter @gennety/db db:push
pnpm db:drift-check   # должен вернуть 0 до pm2 restart
```

**Шесть вещей, которые стоит знать до рестарта:**

- **Фича уезжает ИНЕРТНОЙ, и включать её этим деплоем нельзя.**
  `CLIENT_EVENTS_ENABLED` по умолчанию `false`, в прод-`.env` строки нет, и
  маршрут отвечает 404 (гейт стоит ДО авторизации). Причина не в осторожности:
  privacy manifest приложения **сейчас заявляет, что аналитических данных мы
  не собираем**. Флип флага — связка из трёх правок в одном ходе (env +
  `App/PrivacyInfo.xcprivacy` + анкета App Privacy в App Store Connect), и
  порядок обратный обычному: сначала манифест и анкета, потом флаг.
- **Ночной свип ретеншена получает пятую таблицу** — `client_events` старше
  **90 дней**, по `receivedAt`. Это тот же батч ≤1000 строк/таблицу/тик, нового
  крона нет. До включения флага таблица пуста, то есть свип бесплатен.
- **Строка лога ретеншена поменяла форму** — в неё добавился `clientEvents=N`.
  Пока флаг выключен, строка не печатается вовсе (сумма нулевая).
- **`optionalAuth` — новая middleware, и она нужна ровно одному маршруту.**
  Не подключать её никуда больше не глядя: она НЕ отказывает при битом или
  протухшем токене, что для любого другого эндпоинта означало бы тихую потерю
  авторизации вместо 401.
- **Клиентская половина уже написана и ждёт этот эндпоинт** (iOS-репо, задача
  6.2), но её транспорт — заглушка за протоколом. Живой отправки после этого
  деплоя не будет ни при каком флаге, пока в iOS-репо не подключат настоящий
  транспорт и не выйдет сборка.
- **Контракт прогнан через генератор Swift-клиента**, а не только через
  `openapi:lint`: тот этот класс дефекта не видит (DECISIONS 2026-08-10).
  Ноль строк `Schema "null" is not supported`, операция `postClientEvents`
  сгенерирована, enum типов приехал без `_empty_`.

Preflight по этому изменению: typecheck чист по 5 проектам, `openapi:lint`
valid (9 warnings — базовая линия), **полный набор 4407 тестов** (бот 3805 /
shared 280 / webapp 322), 0 failed. Полный прогон здесь не формальность — он
уронил 12 тестов в `workers/retention.test.ts`, файле, который правка не
трогала (DECISIONS.md).

Проверка после деплоя — при выключенном флаге правильный ответ 404, и это же
доказывает, что маршрут смонтирован:

```sh
curl -s -o /dev/null -w '%{http_code}\n' -X POST \
  https://dating-api.gennety.com/v1/client/events \
  -H 'content-type: application/json' -d '{"installId":"probe","events":[]}'
# 404 = смонтирован и корректно инертен. 200 = флаг включён раньше манифеста.
psql "$DATABASE_URL" -c "select count(*) from client_events;"
# Таблица существует и пуста. Ошибка «relation does not exist» = db:push не прошёл.
```

**Rollback:** откатить код и перезапустить. Таблица может остаться — старый код
её не читает; свип ретеншена исчезает вместе с кодом.

---

**Deployed 2026-08-20 — релиз из 49 коммитов: все 26 PENDING-блоков разом
(`c577665`).** Полный деплой кода + аддитивный `db:push` + Mini App + демо.
Прод поднят с `cd25c56` до `c577665`.

Порядок: worktree-проверка якоря → preflight → `migrate diff` → rsync →
install/build → `db:push` → `db:drift-check` → `pm2 restart` →
`deploy-webapp.sh` → `demo:deploy`.

**Схема:** ровно один `ADD COLUMN` (`curated_venues.photo_refs`), **ноль
DROP** — план прочитан `migrate diff` и локально, и на дроплете до запуска.
`db:drift-check` **OK**.

**Якорь подтверждён побайтово с обеих сторон.** До деплоя: прод == `cd25c56`,
791 файл, ноль расхождений. После: прод == `c577665`, 814 файлов, единственные
четыре расхождения — `apps/bot/tmp/*`, который rsync исключает по дизайну (те же
четыре, что и в релизе 2026-08-07).

**Главное содержимое релиза — два бага одного класса «строка, невидимая своему
же механизму починки»:**

- **§3.3 — матч, не доехавший ни до кого, перестаёт быть бессмертным.**
  `disposeUndeliveredMatch`; в проде до деплоя таких строк было **0**
  (проверено `pnpm audit:stuck-matches --prod`), после — тоже 0.
- **§3.5b — дедлайн тикет-гейта армируется переходом**, а не карточкой.
- **§3.1c** получает предусловие: пересев синтетиков — только на деплой с
  `disposeUndeliveredMatch`.

**Демо задеплоено и проверено отдельно** (первый заход оборвался, см. ниже):
схема применена к демо-БД (`aws-1-eu-west-1` — не продовая `aws-0`),
`db:drift-check` OK, рестарт 27 → **28**, баннер изоляции называет
`@gennety_demo_bot` и демо-базу, оба демо-подавления кронов на месте
(`Drop matching NOT scheduled`, `No-match notice NOT scheduled`), демо-бандл
Mini App собран с `VITE_API_BASE_URL=https://demo-api.gennety.com` и выложен,
`demo-app/onboarding.html` 200, `demo-api` ping ok, ноль ошибок от нового PID.

**⚠️ И одна ловушка, в которую этот релиз чуть не наступил.** Доводя демо
руками, легко забыть последний шаг самого `deploy-demo.sh`: он пересобирает
`dist/` ОБРАТНО на прод-базу. Без этого локальный `apps/webapp/dist/` остаётся
demo-указывающим, и следующий `deploy-webapp.sh` отправит demo-бандл на
**боевой** хост. Проверка — по содержимому, а не по памяти:

```sh
grep -rlo 'demo-api.gennety.com' apps/webapp/dist/assets/ | wc -l   # обязан быть 0
grep -rlo 'dating-api.gennety.com' apps/webapp/dist/assets/ | wc -l # > 0
```

**⚠️ Две операционные вещи, которые стоит знать до следующего раза:**

- **`pnpm build` на дроплете переживает разрыв ssh, а `deploy-demo.sh` — нет.**
  Первый запуск демо-деплоя оборвался ровно на `vite build` и **рестарта не
  сделал** (restart count не сдвинулся — это и есть признак). Причина не OOM:
  памяти 894 МБ свободно, в dmesg чисто; сборка Remotion + Vite на 1–2 vCPU
  просто идёт минутами, дольше любого разумного таймаута команды. Запускать под
  `nohup` и ждать по исчезновению процесса, а **не** по появлению строки в логе.
- **Полный набор тестов нестабилен под параллелизмом.** Один прогон из трёх дал
  2 падения (`calendar-native.test.ts` 404→400 и таймаут `DELETE /v1/me` на
  10 с); оба файла проходят изолированно, следующие два полных прогона —
  4376/4376 зелёных. Это тот же класс, что задокументирован для
  `date-card/render.test.ts` 2026-08-02. **Красный preflight с этими двумя
  именами — перепрогнать, а не чинить.** Настоящее лечение — отдельный
  low-concurrency vitest-проект для тяжёлых файлов; это решение про воркфлоу, и
  оно не принято.

Preflight: typecheck чист по 5 проектам, lint чист, **4376 тестов** (бот 3774 /
shared 280 / webapp 322) — 0 failed, `pnpm build` чист, `security:secrets`
(1120 файлов), **`security:audit` 0 advisories**, дерево чистое и вровень с
`origin/main`.

rsync dry-run показал **11** удалений, все прочитаны и сверены с
`git log --diff-filter=D`: 2 устаревших артефакта `apps/video/build` плюс 9
файлов, удалённых коммитом `dda1d2e` (три PNG конкурентов + их каталог,
`seeded-noise.ts`, `onboarding-money.ts` + тест, `onboarding-crumble.ts` + тест).
Оба ключа `keys/*.p8` и оба бэкапа БД проверены на месте ПОСЛЕ синка.

**Проверено на живом проде (замерено, а не выведено):**

- `Bot @gennetybot started`, **все 18 кронов** зарегистрированы, включая
  ежевечерний `Drop matching "0 18 * * *"` и `Synthetic test partner`;
  `:3100`/`:3101` слушают.
- Рестарт 63 → **64**, `unstable restarts: 0`, **ноль** `P2022` / `P2023` /
  `ERR_MODULE_NOT_FOUND` / unhandled от нового PID.
- **`/admin/stats` отдаёт новое поле:** `matches.strandedProposed: 0` рядом с
  `live: 0`. Это и есть проверка §3.3 без запуска скрипта.
- `disposeUndeliveredMatch` присутствует в задеплоенном файле (3 вхождения).
- `/v1/ping` ok, admin `401`, **все 11 страниц Mini App 200**, и хеш бандла на
  проде совпал с собранным (`onboarding-70-bgwTm.js`) — проверка по хешу, а не
  по коду 200, ровно как предписывает секция про якорь бандла.

**Rollback:** пересинкать чекаут на `cd25c56`, перезапустить, оттуда же
редеплоить Mini App и демо. Аддитивная колонка может остаться — старый код её
не читает.

---

**Deployed 2026-08-20 (was PENDING) — дедлайн тикет-гейта армируется переходом + счётчик застрявших строк
в `/admin/stats` (PRODUCT_SPEC §3.5b / §3.1c, DECISIONS.md).** **Нет изменения
схемы Prisma, нет новых env, нет флагов, нет изменения Mini App** — только бот,
плюс **редеплой дашборда** (отдельный репозиторий), если нужно отрисовать новое
поле; API работает и без него. Едет ОДНИМ релизом с блоком ниже.

- **С выключенными билетами не меняется ничего**, и это проверяемо: запись
  условна по `TICKET_FEATURE_ENABLED`, поэтому payload перехода остаётся
  байт-в-байт прежним (существующий тест на `data: { status: "negotiating" }`
  так и проходит). В проде флаг **включён**, так что там дедлайн начнёт
  проставляться на переходе.
- **Окно гейта у новых матчей начинается чуть раньше** — от момента взаимного
  согласия, а не от отправки карточки. Разница — миллисекунды; `sendTicketOffer`
  всё равно перештампует своё окно.
- **`/admin/stats` получает поле `matches.strandedProposed`.** Обязано быть 0.
  Ненулевое = двое выпали из пула; запрос на отмену — в выводе
  `pnpm audit:stuck-matches --prod`.
- Дашборд читает секции через `.catch(() => null)`, так что старый фронт не
  падает — просто не показывает новое поле.

```sh
KEY=$(ssh root@167.172.178.229 "sed -n 's/^ADMIN_API_KEY=//p' /opt/gennety/.env | tail -1 | tr -d '\"'")
curl -s -H "Authorization: Bearer $KEY" https://api-admin.gennety.com/admin/stats \
  | python3 -c "import json,sys; print(json.load(sys.stdin)['matches'])"
# strandedProposed обязан быть 0.
```

**Rollback:** откатить код и перезапустить. Ни схемы, ни env, ни флага.

---

**Deployed 2026-08-20 (was PENDING) — матч, не доехавший ни до кого, перестаёт быть бессмертным
(PRODUCT_SPEC §3.3, ARCHITECTURE → `matches`, DECISIONS.md).** **Нет изменения
схемы Prisma, нет новых env, нет флагов, нет изменения Mini App**
(`apps/webapp` не тронут) — только бот, так что полный деплой кода несёт это
целиком, плюс `pnpm demo:deploy`.

`dispatchMatches` больше не оставляет строку одновременно живой и без
`dispatchedAt`. Питч дошёл хотя бы до одной стороны — ставим TTL (как раньше);
не дошёл ни до кого — **матч отменяется**, оба слота освобождаются.

**Пять вещей, которые стоит знать до рестарта:**

- **Это чинит молчаливую дыру, а не деградацию.** `dispatchedAt: { not: null }`
  стоит фильтром в свипе истечения, в воркере обратного отсчёта и в обеих
  каденциях напоминаний, поэтому строка была невидима всем сразу, а инвариант
  одного живого матча держал **обоих** участников вне каждого дропа. В проде
  одна такая прожила **123 часа**, и человек по ту сторону пять дней не получал
  ничего (DECISIONS.md 2026-08-20).
- **Появляется новая строка в логе, и она ERROR:**
  `[dispatch] matchId=… reached NEITHER side — cancelled so both participants
  are freed for the next drop`. Её всплеск означает, что продукт создаёт пары,
  до которых не может достучаться, — смотреть надо на достижимость (403,
  синтетики, mobile-стороны), а не на эту правку. Ноль строк — здоровое
  состояние.
- **Возвращать при отмене нечего:** билет на `proposed`-строке висеть не может
  (§3.5b), голый флип статуса — уже принятая форма отмены `proposed` (то же
  делает `decision.ts` на смешанном вердикте), и уведомлений никто не получает
  (никто и не видел карточки).
- **Пожизненный бан пары сохраняется** — строка остаётся, как и при любом другом
  способе разрешения. Эти двое больше не встретятся; так было и до правки.
- **Платный Rematch чинится тем же изменением.** Там покупка помечается
  `settled` ДО рассылки, так что раньше провал доставки означал: деньги списаны,
  питча нет, и покупатель заперт в бессмертном матче — то есть не может даже
  купить ещё раз. Теперь пара отменяется и повторная покупка доступна сразу.
  **Возврат в этом случае НЕ добавлен** — это ценовое решение (§3.11 возвращает
  только «движок никого не нашёл»), и оно не принято.

Preflight по этому изменению: typecheck чист по 5 проектам, lint чист, **полный
набор 4373 теста** (бот 3771 / shared 280 / webapp 322), 0 failed. Оба несущих
гварда подтверждены **красными** по отдельности: возврат прежнего раннего
`return` роняет ровно два теста в `dispatch-queue.test.ts`.

Проверка после деплоя — счастливый путь молчит, поэтому смотрим на отсутствие
бессмертных строк, а не на новую строку в логе:

```sh
pm2 logs gennety-bot --lines 200 --nostream | grep 'reached NEITHER side'
# Пусто = все питчи доезжают. Строки здесь = пары, до которых не достучались.
psql "$DATABASE_URL" -c "select count(*) from matches where status='proposed' and dispatched_at is null;"
# Должно быть 0 и оставаться 0. Ненулевое после деплоя = правка не доехала.
```

**Строки, застрявшие ДО деплоя, сами не рассосутся** — правка forward-only.
Проверить тем же запросом и отменить руками (билетов на них нет, уведомлять
некого):

```sql
UPDATE matches SET status='cancelled'
 WHERE status='proposed' AND dispatched_at IS NULL;
```

**Rollback:** откатить код и перезапустить. Откатывать больше нечего — ни схемы,
ни env, ни флага; откат возвращает бессмертные строки.

---

**Deployed 2026-08-20 (was PENDING) — фотографии в Mini App перестают блокироваться CORP (ARCHITECTURE.md
→ «Cross-origin image proxies», DECISIONS.md).** **Нет изменения схемы Prisma,
нет новых env, нет флагов, нет изменения Mini App** (`apps/webapp` не тронут) —
только бот, так что полный деплой кода несёт это целиком, плюс `pnpm demo:deploy`.

Аватары на кнопке оплаты билета и обложки карточек на доске смены места не
рисовались: `helmet()` ставит `Cross-Origin-Resource-Policy: same-origin` на
каждый ответ публичного API, а Mini App живёт на другом хосте, чем API. Три
маршрута-прокси (карт-тайлы, аватары билета, фото доски) теперь снимают заголовок
через общий `public/cross-origin-image.ts`.

**Пять вещей, которые стоит знать до рестарта:**

- **Это НЕ баг демо, хотя пришло как отчёт про демо.** Прод отдаёт тот же
  `same-origin` — проверено на живом `dating-api`, — то есть аватары билета и
  фотографии доски сломаны и там. Не исполнялись ни разу: 0 свиданий и 0 проданных
  билетов за всю историю, так что до этих экранов в проде никто не доходил.
- **Два предыдущих фикса этого симптома причиной не были** (сжатие аватаров
  2026-08-07, ретраи прокси 2026-08-08). Оба — настоящие улучшения и оба
  остаются; ни один не откатывается.
- **Расширяется ровно один заголовок на трёх маршрутах.** Ослабление CORP
  сознательно НЕ применено к медиа фаундерского отчёта (та же origin), к
  реферальной карточке (её тянут серверы Telegram) и к
  `/v1/matches/partner-photo` (нативный iOS). Список и причины — в
  `cross-origin-image.ts`.
- **Проверять надо в БРАУЗЕРЕ, а не curl'ом.** Ни curl, ни серверный пробник, ни
  supertest CORP не применяют — именно поэтому отказ пережил две диагностики.
  Единственная надёжная проверка на сервере — сам заголовок (команда ниже).
- **Прокси карт-тайлов ставил заголовок и раньше**, инлайном; правка только
  переводит его на общий модуль, поведение то же.

Preflight по этому изменению: typecheck чист по 5 проектам, lint чист, **полный
набор бота 3769 тестов / 245 файлов, 0 failed** (+2 новых). Оба гварда
подтверждены красными по отдельности: снятие вызова роняет ровно один тест в
`ticket-api.test.ts` и один в `venue-change-api.test.ts`.

Проверка после деплоя — заголовок виден напрямую, а сквозная проверка глазами в
демо (дойти до экрана оплаты билета: на кнопке «Сплатити за двох» обязаны быть
две ФОТОГРАФИИ, а не буквы):

```sh
pnpm demo:deploy
# Работающий эталон и починенный маршрут обязаны совпасть по заголовку:
curl -sI https://dating-api.gennety.com/v1/maptiles/3/4/2 | grep -i cross-origin-resource
curl -sI https://demo-api.gennety.com/v1/maptiles/3/4/2  | grep -i cross-origin-resource
# Аватар требует подписанной initData, поэтому проще смотреть глазами в демо;
# `same-origin` на любом из двух прокси изображений = фикс не доехал.
```

**Rollback:** откатить код и перезапустить. Откатывать больше нечего — ни схемы,
ни env, ни флага, ни состояния; откат возвращает монограммы вместо фотографий.

---

**Deployed 2026-08-20 (was PENDING) — галерея и полноэкранный просмотр площадки сводятся на одну ширину
(PRODUCT_SPEC §3.7b, DECISIONS.md).** **Нет изменения схемы Prisma, нет новых
env, нет флагов и НЕТ ИЗМЕНЕНИЙ СЕРВЕРА ВООБЩЕ** — дифф это
`apps/webapp/**` плюс документация. Путь **Deploy Mini App Only**
(`./scripts/deploy-webapp.sh`); рсинкать в `/opt/gennety` нечего, **`pm2 restart`
не нужен**. Обязательно ещё `pnpm demo:deploy` — демо собирает свой бандл из
того же исходника.

Ширина входит в URL фото-прокси, то есть это и ключ кэша на клиенте, и отдельно
тарифицируемый запрос Place Photo наверх. Галерея просила 1000, полноэкранный
просмотр — 1600, поэтому **каждая увеличенная фотография покупалась дважды**.
Теперь обе берут `VENUE_PHOTO_WIDTH` (1200), и просмотрщик рисует из кэша.

**Три вещи, которые стоит знать до редеплоя:**

- **Галерея станет чуть тяжелее по байтам и это ничего не стоит.** Place Photo
  тарифицируется за запрос, а не за байты, так что 1000 → 1200 в счёте не видно
  вовсе, а убирает вторую покупку целиком.
- **1200, а не 1000, выбрано ради полноэкранного случая**: у телефона 390pt при
  DPR 3 это 1170 физических пикселей. Просто снять 1600 значило бы сделать
  полноэкранный просмотр мягче, чем он был.
- **Превью карточки (240) намеренно осталось своей шириной** — это другая
  картинка другого размера, и их на доске 21 против нескольких открытых.

Preflight по этому изменению: typecheck вебаппа чист, **тесты вебаппа зелёные**
(+1 новый файл, 3 проверки). Гвард подтверждён красным: возврат второй ширины
роняет `venue-photo-width.test.ts`.

Проверка после редеплоя — экран транзиентный и ничего не логирует, поэтому
глазами плюс по сети:

```sh
./scripts/deploy-webapp.sh
pnpm demo:deploy
# В демо: дойти до назначенного свидания, открыть «Сменить место», открыть
# карточку площадки, тапнуть по фотографии. В DevTools → Network на открытие
# полноэкранного просмотра НЕ должно уходить ни одного нового запроса к
# /v1/venue-change/photo — если уходит с `w=1600`, уехал старый бандл.
```

**Rollback:** передеплоить Mini App с предыдущего чекаута и оттуда же
`pnpm demo:deploy`. Откатывать больше нечего — ни схемы, ни env, ни флага, ни
серверного состояния.

---

**Deployed 2026-08-20 (was PENDING) — фотографии площадок переезжают из памяти процесса в БД
(PRODUCT_SPEC §3.7a, ARCHITECTURE → `curated_venues`, DECISIONS.md).** **Нет
новых env, нет флагов, нет изменения Mini App** (`apps/webapp` не тронут) — но
нужен **аддитивный `db:push` ДО рестарта**, так что порядок: Deploy Full Server
Code → `db:push` → `pnpm db:drift-check` → `pm2 restart` → `pnpm demo:deploy`.

Одна новая колонка, `curated_venues.photo_refs` (`String[] @default([])`),
**читается на каждом открытии доски** и пишется кроном ре-валидации, поэтому
база без неё бросит `P2022` — тот самый крэш-луп PM2, о котором предупреждает
этот файл. Сначала убедиться, что план аддитивный (ожидается один `ADD COLUMN`,
ноль `DROP`):

```sh
export DATABASE_URL="$(sed -n 's/^DATABASE_URL=//p' .env | tail -1 | tr -d '"')"
pnpm --filter @gennety/db exec prisma migrate diff \
  --from-schema-datasource prisma/schema.prisma \
  --to-schema-datamodel prisma/schema.prisma --script
pnpm --filter @gennety/db db:push
pnpm db:drift-check   # должен вернуть 0 до pm2 restart
```

Что меняется: крон `venue-revalidation` начинает просить у Place Details поле
`photos` и складывать ссылки на строку площадки, а доска смены места читает их
оттуда вместо собственного вызова на каждую площадку. Раньше эти ссылки жили
только в памяти процесса, то есть каждый деплой выбрасывал город целиком.

**Пять вещей, которые стоит знать до рестарта:**

- **Расход на Google не растёт, а падает, и это не предположение.** Place Details
  тарифицируется по самому дорогому запрошенному полю, а не по сумме, а тот же
  запрос уже просит `rating`/`regularOpeningHours`. То есть добавление `photos`
  стоит максимум столько же, сколько запрос стоил, и убирает отдельный вызов
  `fetchPlacePhotoNames` — вывод верен при любом тарифе поля `photos`.
- **Заполняется не мгновенно: ~9 дней на полный цикл по Киеву** (269 активных
  площадок при 30 за ночной тик). Всё это время `withCuratedPhotos` работает
  запасным путём ровно как раньше, поэтому пользователь разницы не видит —
  просто расход снижается постепенно, а не в день деплоя.
- **`prisma db:generate` обязателен** (входит в Deploy Full Server Code): без
  перегенерации клиента `select: { photoRefs: true }` упадёт.
- **Импортёр каталога и `sync-venues:kyiv` колонку не трут** — проверено, в их
  update-payload её нет, а Prisma пишет только перечисленные поля. Так что
  `seed-venues:import --apply` после деплоя безопасен и refs переживёт ре-синк.
- **В проде это ещё нечем исполнить**: до доски не дошла ни одна пара (0 свиданий
  за всю историю). Крон при этом работает и начнёт заполнять колонку сразу —
  проверять надо именно его, а не доску.

Preflight по этому изменению: typecheck чист, **тесты бота по затронутым файлам
зелёные** (venue-revalidation 9, venue-change 65, +3 новых). Оба несущих гварда
подтверждены красными по отдельности: запись пустого ответа поверх сохранённых
refs роняет тест ре-валидации, а возврат `photoRefs: []` в каталоге роняет тест
чтения.

Проверка после деплоя — колонка должна начать заполняться в первую же ночь:

```sh
psql "$DATABASE_URL" -c "select count(*) filter (where cardinality(photo_refs) > 0) filled, count(*) total from curated_venues where active;"
# 0 сразу после деплоя, растёт примерно на 30 за ночь. Через ~9 дней ≈ total.
pm2 logs gennety-bot --lines 200 --nostream | grep 'venue-revalidation'
# Строки с refreshed=N — крон отработал. failed=N означает сбой Places, не эту правку.
```

**Rollback:** откатить код и перезапустить. Колонку можно оставить — старый код
её не читает, а `withCuratedPhotos` возвращает доску к прежнему поведению.

---

**Deployed 2026-08-20 (was PENDING) — закреплённый баннер перестаёт опережать карточку свидания
(PRODUCT_SPEC §2.1, DECISIONS.md).** **Нет изменения схемы Prisma, нет новых env,
нет флагов, нет изменения Mini App** (`apps/webapp` не тронут) — только бот, так
что полный деплой кода несёт это целиком, плюс `pnpm demo:deploy`.

Пуш баннера при первом назначении места уехал с самого начала
`deliverScheduledConfirmation` на `.finally` каждой персональной отправки. До
этого пин показывал уже назначенные дату и место, пока в чате ещё шёл шиммер
«Собираю карточку свидания…» — не гонка, а гарантированные **семь секунд и
больше**: `dateCardSteps` идёт `NEVER_CUT_SHORT` ~6.3 с плюс генерация blurb.

**Четыре вещи, которые стоит знать до рестарта:**

- **Баннер теперь обновляется ПОЗЖЕ, чем раньше, и это и есть исправление.**
  Раньше он ехал сразу после коммита `scheduled`, теперь — после того, как
  карточка (или её текстовый fallback) уже в чате. Смотреть надо на порядок на
  экране, а не на «быстро ли обновился пин».
- **Пуш стал per side: два вызова `refreshStatusBanners` вместо одного.** Это
  второй `findMany` + `loadBannerStages` на событие, которое случается один раз
  за свидание — практически ноль. Взамен одна недостижимая сторона больше не
  задерживает и не отменяет баннер второй.
- **В проде это нечем исполнить.** До `scheduled` не дошла ни одна пара (0
  свиданий за всю историю), так что проверять надо в демо — там каждый прогон
  доходит до назначенного свидания — либо на `@gennetytestbot`.
- **Демо наследует поведение без единой ветки**: ни гейта, ни платного шага, ни
  решения для куклы; `apps/bot/src/demo/decide.ts` не тронут, beat с передачей
  дата-карточки работает как работал.

Preflight по этому изменению: typecheck чист, lint чист, **полный набор бота
3758 тестов / 244 файла, 0 failed**. Оба гварда подтверждены красными по
отдельности: возврат к общему раннему пушу роняет per-side проверку, а per-side
пуш ДО отправок роняет проверку порядка (`expected 8 to be greater than 11`).

Проверка после деплоя — счастливый путь ничего не логирует, поэтому глазами в
демо: дойти до назначения места и смотреть на закреплённое сообщение, пока идёт
шиммер «Собираю карточку свидания…». Пин обязан оставаться в режиме «планируем»
(без обратного отсчёта) до появления карточки и переключиться на отсчёт с
названием места только после неё. Косвенно:

```sh
pnpm demo:deploy
pm2 logs gennety-bot --lines 200 --nostream | grep 'push refresh failed'
# Пусто = пуши не падают. Строки здесь означают, что баннер отдан тику —
# отставание до минуты, не сломанное состояние.
```

**Rollback:** откатить код и перезапустить. Откатывать больше нечего — ни схемы,
ни env, ни флага, ни состояния; откат возвращает опережающий пин.

---

**Deployed 2026-08-20 (was PENDING) — «made with Gennety» больше не уезжает за край дата-карточки
(PRODUCT_SPEC §3.7a, DECISIONS.md).** **Нет изменения схемы Prisma, нет новых
env, нет флагов, нет изменения Mini App** (`apps/webapp` не тронут) — только бот,
так что полный деплой кода несёт это целиком, плюс `pnpm demo:deploy`.

Кредит переехал из строки рядом с адресом в левый нижний угол фотографии места
(абсолютное позиционирование), а название и адрес площадки клипаются в одну
строку с многоточием.

**Пять вещей, которые стоит знать до рестарта:**

- **Это чинит больше половины реальных площадок, а не редкий край.** Порог
  поломки — ~45 символов адреса, а по живому киевскому каталогу медиана 45 и
  p90 64. На присланном 57-символьном адресе кредит уезжал на **x=1127 при
  ширине карточки 1080** — то есть просто обрезался.
- **Меняется вид КАЖДОЙ карточки, а не только сломанных**: кредит теперь на
  фотографии у всех. Это осознанно — условное размещение отклонено (satori не
  отдаёт метрики текста до рендера, см. DECISIONS.md).
- **Правку наследует и карточка-желание смены места** (`venue-wish-card.ts`),
  которая переиспользует ровно этот макет. Там слогана две строки вместо трёх,
  то есть слабины по вертикали на 78px больше — правка там строго безопаснее.
- **Очень длинные названия площадок теперь обрезаются многоточием.** По каталогу
  медиана названия 13 символов, p90 28, так что это касается хвоста вроде
  «KOFEiN Sumska, str, 63 Kharkiv, Kharkivska obl 61000». Полное имя и полный
  адрес печатает подпись под фотографией — на карточке ничего не теряется.
- **В проде это исполняется редко**: свиданий за всю историю 0, а
  `DATE_CARD_FEATURE_ENABLED` гейтит саму карточку. Проверять в демо (там каждый
  прогон доходит до назначенного свидания) или на `@gennetytestbot`.

Preflight по этому изменению: typecheck бота чист, lint чист по всем 5 проектам,
**18 тестов `src/services/date-card` зелёные** (+5 новых в `template.test.ts`).
Четыре новых утверждения подтверждены **красными** на старом коде — два
геометрических (боксы на x=1127 и x=1204 за краем карточки) и два структурных.

Проверка после деплоя — путь ничего не логирует на счастливой ветке, поэтому
глазами: в демо дойти до назначенного свидания и убедиться, что подпись «made
with Gennety» лежит на фотографии слева внизу, а адрес под фотографией никуда не
уезжает. Косвенно:

```sh
pnpm demo:deploy
pm2 logs gennety-bot --lines 200 --nostream | grep '\[date-card\]'
# Пусто = ни один рендер не упал.
```

**Rollback:** откатить код и перезапустить. Откатывать больше нечего — ни схемы,
ни env, ни флага, ни состояния.

---

**Deployed 2026-08-20 — четыре портрета Type Radar заменены (PRODUCT_SPEC → §Type Radar,
DECISIONS.md).** **Нет изменения схемы Prisma, нет новых env, нет флагов.** Правка
пользовательской части целиком в бандле Mini App, поэтому путь — **Deploy Mini App
Only** (`./scripts/deploy-webapp.sh`) плюс `pnpm demo:deploy`.

Заменены `f04.jpg` `f07.jpg` `f10.jpg` `m12.jpg` в `apps/webapp/public/radar/a/`.
Бэкап прежних четырёх — `~/Desktop/gennety-backups/radar-a-2026-08-20/` (вне репозитория,
чтобы его не снёс rsync деплоя).

**Четыре вещи, которые стоит знать:**

- **`pm2 restart` для этого НЕ нужен.** В `packages/shared/src/type-radar.ts` изменилось
  только поле `scene` у `f10`/`m12` (`park` → `cafe`), а оно **не читается кодом**: не
  сериализуется в колоду, не участвует в подсчёте предпочтений, не влияет ни на один
  маршрут. Это проектная пометка, и она уедет ближайшим обычным серверным релизом
  вместе с блоками ниже.
- **Формат новых файлов — 562×1000**, а не 750×1000, как у большинства набора. Это
  сознательно: `.radar-card` тянется по высоте и режет по `cover` от центра, поэтому
  источник 3:4 обрезался бы **по бокам**, где находятся руки, а руки несут признак
  `tattoos`. Соотношение форматов в папке стало 14 широких / 10 узких.
- **Сцена больше не 4/4/4**, а 5 cafe / 4 street / 3 park в каждом наборе — принято
  осознанно (DECISIONS.md). Тест `type-radar.test.ts` держит то, что действительно
  важно: каждое значение признака встречается минимум в двух сценах.
- **`TYPE_RADAR_ENABLED=true` в проде**, так что новые карточки увидит первый же
  пользователь, дошедший до шага радара в онбординге.

Preflight: typecheck shared чист, **280 тестов shared** (в т.ч. 22 в `type-radar.test.ts`)
+ **27 тестов бота** по радару — 0 failed. Оба JSON провалидированы.

Проверка после деплоя — карточки транзиентные и ничего не логируют, поэтому по хешу
ассетов и глазами:

```sh
./scripts/deploy-webapp.sh
pnpm demo:deploy
for p in f04 f07 f10 m12; do
  curl -sI "https://dating-calendar.gennety.com/radar/a/$p.jpg" | head -1
done
# Затем на @gennetytestbot или в демо пройти онбординг до шага «выбери свой типаж»
# и убедиться, что #4/#7/#10/#24 — новые кадры.
```

**Проверено после деплоя (замерено, а не выведено):** все четыре файла отдаются с
прода и из демо с `200` и **побайтово тем же размером**, что локально (114262 /
120380 / 122039 / 90217). Соседние кадры не тронуты — `f01` по-прежнему 155297,
`m10` 95915. Демо переехало тем же исходником, изоляция подтверждена его баннером
(`@gennety_demo_bot`, база `aws-1-eu-west-1` против продовой `aws-0-`, оба
демо-подавления кронов на месте), рестарты демо 27, `unstable restarts: 0`,
`demo-api` ping ok. **Продовый бот не перезапускался** — счётчик рестартов
`gennety-bot` держится на 63, как и должно быть при деплое только Mini App.

Строки `chat_events_user_id_fkey` в error-логе демо к этому деплою отношения не
имеют: последняя запись в файле датирована 01:55 UTC, деплой был в 05:37. Это
известная давняя история — рекордер таймлайна пишет событие для аккаунта, который
`/restart` только что удалил; ошибка проглатывается.

**Rollback:** вернуть четыре файла из бэкапа выше, передеплоить Mini App и демо.
Откатывать больше нечего — ни схемы, ни env, ни флага.

---

**Deployed 2026-08-20 (was PENDING) — интро сокращено до трёх экранов: шесть сцен критики рынка удалены
(PRODUCT_SPEC §1.1, DECISIONS.md).** **Нет изменения схемы Prisma, нет новых
env, нет флагов и НЕТ ИЗМЕНЕНИЙ СЕРВЕРА ВООБЩЕ** — диф это `apps/webapp/**`
плюс три комментария в `apps/bot` и документация. Путь **Deploy Mini App Only**
(`./scripts/deploy-webapp.sh`); рсинкать в `/opt/gennety` нечего,
**`pm2 restart` не нужен**. Обязательно ещё `pnpm demo:deploy` — демо собирает
свой бандл из того же исходника.

Из интро вырезаны шесть сцен: подъём иконок конкурентов, их осыпание, «сколько
стоит найти отношения в 2026» с падающими купюрами, барабан статистики, «только
3% доходят до свидания» и свайп-симулятор. Остались Pivot → Matchmaker →
How-it-works. **Автосцены ~50 с → ~15 с** (замерено на живом рендере: границы
сцен на 7.7 с и 15.2 с).

**Шесть вещей, которые стоит знать до редеплоя:**

- **Первое, что увидит пользователь, полностью меняется.** Вместо иконок
  Bumble/Tinder/Badoo интро открывается строкой «Знакомства должны вести к
  встрече, а не к переписке» и поднимающимся логотипом. Это самая заметная
  правка релиза; проверять надо глазами, а не по логам.
- **Прогресс интро в DeviceStorage НЕ мигрирован** (осознанно, DECISIONS.md).
  Аккаунт, застрявший в СЕРЕДИНЕ старого интро со значением ≥ 3, после деплоя
  пропустит интро целиком, а 0–2 попадёт на три выживших экрана. Сломанного
  экрана не бывает ни при каком значении. В проде таких аккаунтов нет (все
  восемь, прошедших контактный гейт, дошли до конца), но это единственное
  необратимое следствие релиза.
- **How-it-works перестаёт обещать импорт памяти из ChatGPT** во всех пяти
  локалях — обещание не выполнялось с 2026-07-26, когда
  `AI_MEMORY_EXPORT_ENABLED` выключили. Держится тестом; вернуть строку можно
  только вместе с фичей.
- **Бандл худеет: onboarding 130.3 → 111.1 kB** (−17%), CSS 4176 → 3217 строк,
  минус три PNG. Диф: −2540 строк, +117.
- **Позиция интро в маршруте НЕ менялась** (решение фаундера): оно по-прежнему
  играет после города и темы, перед экранами профиля. Если после деплоя
  покажется, что интро «пропало» — проверять надо гейты выше, а не эту правку.
- **`@keyframes iconFloat` намеренно оставлен** в CSS: он анимирует живой
  `.pivot-logo-icon`. Единственный keyframe из удалённого иконочного блока,
  который обязан был выжить.

Preflight по этому изменению: typecheck чист, lint чист, **314 тестов вебаппа в
32 файлах** (0 failed), `vite build` чист. Оба новых гварда подтверждены
красными до правок (длина интро и запрет ChatGPT в How-it-works).

Проверка после редеплоя — экраны транзиентные и ничего не логируют, поэтому
глазами либо через dev-превью, которому не нужны ни Telegram, ни аккаунт:

```sh
./scripts/deploy-webapp.sh
pnpm demo:deploy
curl -s https://dating-calendar.gennety.com/onboarding.html \
  | grep -o '/assets/onboarding-[A-Za-z0-9_-]*\.js'
# хеш обязан совпасть с ls apps/webapp/dist/assets/onboarding-*.js в задеплоенном
# дереве; страница, отдающая 200 со старым хешем, — это старый бандл.
# Дев-превью, обе темы (сцены 0/1/2):
#   http://localhost:5173/onboarding.html?preview=intro&lang=ru&theme=dark
#   http://localhost:5173/onboarding.html?preview=intro:1&lang=ru&theme=light
#   http://localhost:5173/onboarding.html?preview=intro:2&lang=ru&theme=dark
# Смотреть на четыре момента: (1) интро открывается новой строкой Pivot, без
# иконок конкурентов; (2) логотип поднимается и ПЕРЕЖИВАЕТ переход на
# Matchmaker; (3) на How-it-works первый шаг говорит «Пара минут в чате», а не
# про ChatGPT; (4) три точки-индикатора и кнопка «Дальше» на месте.
```

**Rollback:** передеплоить Mini App с предыдущего чекаута и оттуда же
`pnpm demo:deploy`. Откатывать больше нечего — ни схемы, ни env, ни флага, ни
серверного состояния. Значения прогресса, записанные новой шкалой (0–3),
старый код прочитает как «интро не пройдено» и проиграет старые девять сцен —
безопасно, но заново.

---

**Deployed 2026-08-20 (was PENDING) — новые актёры на фото второго экрана «Как это работает»
(`apps/webapp/public/how-it-works/2.jpg`).** **Нет изменения схемы Prisma, нет
новых env, нет флагов и НЕТ ИЗМЕНЕНИЙ СЕРВЕРА ВООБЩЕ** — один ассет в
`apps/webapp/public/`. Путь **Deploy Mini App Only**
(`./scripts/deploy-webapp.sh`); рсинкать в `/opt/gennety` нечего, **`pm2 restart`
не нужен**. Плюс `pnpm demo:deploy` — демо собирает свой бандл из того же
исходника и иначе останется со старым фото.

Замена по просьбе прежних актёров (сменился фотограф). Кадр тот же — пара
крупным планом, снек на пальцах, вечерняя вспышка, — люди новые.

**Две вещи, которые стоит знать до редеплоя:**

- **Формат сменился с квадрата 2000x2000 на вертикаль 1152x2048, намеренно.**
  Рамка `.howitworks-photo` тянется по остатку высоты (`flex: 1 1 auto`) и
  кроет `object-fit: cover`, то есть на телефоне она вертикальная (~0.62) — и
  квадрат резался по бокам. Вертикаль теряет только фон сверху и снизу. Апскейл
  не нужен: рамка ~361 CSS-px, при DPR 3 это ~1083 px.
- **Вес упал 502 -> 450 КБ** (JPEG q65). Выше по качеству плёночное зерно
  раздувает файл за прежние 502 КБ, не добавляя видимой разницы.

Проверка после редеплоя — экран статичный и ничего не логирует, поэтому глазами
(и по хешу ассета, а не по коду 200 — именно эту ошибку файл фиксировал 15
августа):

```sh
./scripts/deploy-webapp.sh
pnpm demo:deploy
curl -s https://dating-calendar.gennety.com/how-it-works/2.jpg | md5
# должен совпасть с: md5 -q apps/webapp/public/how-it-works/2.jpg
```

**Rollback:** `git revert` коммита с фото и передеплоить Mini App + демо.
Откатывать больше нечего — ни схемы, ни env, ни флага, ни серверного состояния.

---

**Deployed 2026-08-20 (was PENDING) — стрелка «Назад» в онбординге: графит + свечение от краёв внутрь
(DECISIONS.md).** **Нет изменения схемы Prisma, нет новых env, нет флагов и НЕТ
ИЗМЕНЕНИЙ СЕРВЕРА ВООБЩЕ** — диff это `apps/webapp/**` плюс документация. Путь
**Deploy Mini App Only** (`./scripts/deploy-webapp.sh`); рсинкать в
`/opt/gennety` нечего, **`pm2 restart` не нужен**. Обязательно ещё
`pnpm demo:deploy` — демо собирает свой бандл из того же исходника и иначе
останется на старом.

`.howitworks-back` — стрелка, листающая последние экраны онбординга («как это
работает» и разбор «Подробнее») — была серым стеклом с градиентом. Стала
глубоким графитом `#1b1d20`, безрамочной, освещённой домашним inner-edge sheen
(четыре inset-тени с отрицательным spread — свет идёт от границ к центру).

**Четыре вещи, которые стоит знать до редеплоя:**

- **Кнопка меняется и в СВЕТЛОЙ теме, причём сильнее, чем в тёмной.** Правило
  `:root[data-theme="light"] .howitworks-foot .howitworks-back` (белое
  матовое стекло с чернильной стрелкой) **удалено**: теперь на кремовой
  странице это тот же тёмный графит. Так и просили, и это не побочный эффект —
  свету, текущему внутрь, на бледной заливке нечего заливать. Та же логика,
  по которой `--pref-both-bg` чёрный в обеих темах.
- **Кнопка живёт на ДВУХ поверхностях**, и вторую легко не проверить: экран
  «как это работает» (тема страницы, под фото-героем) и разбор «Подробнее»
  (`.howitworks-dock`, над всегда-тёмным размытым фото). Вторая раньше и так
  была тёмной в обеих темах, так что там видимая разница минимальна — но
  смотреть надо обе.
- **Токены `--back-graphite` / `--back-sheen` объявлены один раз на `:root`**,
  без вариантов по теме. Тест это держит: повторное объявление или любое
  правило `[data-theme] … .howitworks-back` роняет
  `back-button-sheen.test.ts`.
- **Свечение слабее, чем у `.ref-share` и барабана роста, и это намеренно**
  (правка по замечанию фаундера, до первого деплоя): альфы примерно вдвое ниже.
  На диске 56px та же альфа покрывает куда большую долю фигуры, чем на широкой
  строке, поэтому «как у пиллов» здесь читается как светящаяся кнопка. Сверять
  на глаз с `.ref-share` не надо — они и не должны совпадать по яркости.

Preflight по этому изменению: typecheck чист, lint чист, **336 тестов вебаппа
в 34 файлах** (0 failed, +7 новых), `vite build` чист. Все четыре гварда
подтверждены красными до фикса: положительный spread, вернувшееся кольцо
`inset 0 0 0 1px`, переопределение под светлую тему и осветлённая заливка —
каждое роняет тест.

Проверка после редеплоя — экраны транзиентные и ничего не логируют, поэтому
глазами. Дев-превью не требует ни Telegram, ни аккаунта:

```sh
./scripts/deploy-webapp.sh
pnpm demo:deploy
curl -sI https://dating-calendar.gennety.com/onboarding.html | head -1
# Дев-превью, обе темы (после открытия — один тап «Дальше», чтобы стрелка
# появилась; ещё два тапа доводят до разбора «Подробнее»):
#   http://localhost:5173/onboarding.html?preview=intro:8&lang=ru&theme=dark
#   http://localhost:5173/onboarding.html?preview=intro:8&lang=ru&theme=light
# Смотреть на три момента: (1) в СВЕТЛОЙ теме кнопка тёмная, а не белёсая —
# если она светло-серая, уехал старый бандл; (2) обводки нет, а край изнутри
# подсвечен — свет не выходит за пределы кружка; (3) центр кружка темнее его
# края, но подсветка — именно намёк: если кружок читается как светящийся,
# значит уехала версия до правки яркости.
```

**Rollback:** передеплоить Mini App с предыдущего чекаута и оттуда же
`pnpm demo:deploy`. Откатывать больше нечего — ни схемы, ни env, ни флага, ни
серверного состояния.

---

**Deployed 2026-08-20 (was PENDING) — под клавиатуру освобождает место только экран имени, а не все пять
экранов профиля (PRODUCT_SPEC §1.1, DECISIONS.md).** **Нет изменения схемы
Prisma, нет новых env, нет флагов и НЕТ ИЗМЕНЕНИЙ СЕРВЕРА ВООБЩЕ** — диff это
`apps/webapp/**` плюс документация. Путь **Deploy Mini App Only**
(`./scripts/deploy-webapp.sh`); рсинкать в `/opt/gennety` нечего, **`pm2 restart`
не нужен**. Обязательно ещё `pnpm demo:deploy` — демо собирает свой бандл из того
же исходника.

`--kb-height` вычитался из `.ob-basics` — общей рамки всех пяти экранов профиля,
— поэтому следующий экран наследовал остаток клавиатуры и доигрывал её движение
уже после переключения. Теперь усадка живёт только на `.ob-basics--name`.

**Четыре вещи, которые стоит знать до редеплоя:**

- **Меняется поведение четырёх экранов, а не одного.** Возраст, пол,
  предпочтения и рост перестают реагировать на `--kb-height` вовсе. Замерено на
  экране возраста (375×812): было — число и слайдер **165px**, кнопка **331px**
  движения после переключения; стало — **0** по всем элементам. Экран имени не
  тронут: усадка 330.9px и кнопка едут ровно как после 08-18.
- **Кривая `--kb-ease` НЕ меняется**, и её не надо «чинить»: она правильна для
  экрана с полем. Причина, по которой правка 08-18 проявилась на соседнем
  экране, — не кривая сама по себе, а то, что клавиатура УХОДИТ рампой, а не
  ступенькой (DECISIONS.md: 315px из 336 всё ещё не отыграно в момент, когда
  клавиатуры уже нет, против 43px у прежней кривой).
- **Новое состояние, которого раньше не было: пару сотен миллисекунд кнопка
  «Продолжить» на экране возраста стоит ЗА уходящей клавиатурой.** Это ожидаемо
  и это и есть исправление — экран приезжает сразу на своё место, а клавиатура
  съезжает и открывает его.
- **Гварды в `keyboard-ease.test.ts`, подтверждённые красным** до правки: на
  прежнем CSS новый тест падает («.ob-basics reads --kb-height: true»).

Preflight по этому изменению: typecheck чист, lint чист, **329 тестов вебаппа в
33 файлах** (0 failed, +1 новый), `vite build` чист.

Проверка после редеплоя — экраны транзиентные и ничего не логируют, поэтому
глазами либо через дев-превью, которому не нужны ни Telegram, ни аккаунт:

```sh
./scripts/deploy-webapp.sh
pnpm demo:deploy
curl -s https://dating-calendar.gennety.com/onboarding.html \
  | grep -o '/assets/onboarding-[A-Za-z0-9_-]*\.js'
# хеш обязан совпасть с ls apps/webapp/dist/assets/onboarding-*.js в
# задеплоенном дереве; 200 со старым хешем — это старый бандл.
# Дев-превью (клавиатуры в headless нет, поэтому переменная выставляется руками):
#   http://localhost:5173/onboarding.html?preview=basics:age&lang=ru&theme=dark
#   в консоли: document.documentElement.style.setProperty('--kb-height','336px')
# Ожидается: на экране ВОЗРАСТА не сдвигается ничего. На экране имени
# (?preview=basics:name) кнопка по-прежнему поднимается и опускается плавно.
# На телефоне: ввести имя, нажать «Продолжить» — экран возраста обязан
# появиться сразу собранным, без сползания числа и слайдера вниз.
```

**Rollback:** передеплоить Mini App с предыдущего чекаута и оттуда же
`pnpm demo:deploy`. Откатывать больше нечего — ни схемы, ни env, ни флага, ни
серверного состояния.

---

**Deployed 2026-08-20 (was PENDING) — анимация поиска Rematch (≥10 с) + PNG-карточка на оффере
(PRODUCT_SPEC §3.11, REMATCH_PRODUCT_SPEC.md, DECISIONS.md).** **Нет изменения
схемы Prisma, нет изменения Mini App** (`apps/webapp` не тронут) — только бот,
плюс **одна новая опциональная env, уезжающая ПУСТОЙ**. Полный деплой кода несёт
всё целиком; `pnpm demo:deploy` по стандарту релиза, хотя в демо фича выключена.

После оплаты реметча над прогоном движка играет rich-шиммер из четырёх битов
(ровно 10.0 с, всегда доигрывает целиком), а DM с предложением купить уезжает
карточкой 1080×1080 с подписью и той же кнопкой.

**Шесть вещей, которые стоит знать до рестарта:**

- **`REMATCH_FEATURE_ENABLED=true` в проде, так что это ЖИВОЙ денежный путь** —
  в отличие от большинства блоков выше. Покупок за всю историю **0**, поэтому
  первое исполнение будет первым в принципе; проверять на `@gennetytestbot`.
- **Отказ «никого не нашёл» теперь приходит через ~10 с, а не мгновенно.**
  Неустранимо по конструкции и осознанно (DECISIONS.md): те же десять секунд
  честно показывают, что искали.
- **Новая env `MESSAGE_EFFECT_REMATCH_ID`, по умолчанию пустая.** Фича уезжает
  инертной; id выбирается и ставится в `/opt/gennety/.env` отдельным шагом.
  Строка с комментарием добавлена в `.env.example`. **Не вешать эффект на
  карточку оффера** — §3.5b про декор на экране, который просит денег.
- **Новых ассетов и зависимостей НЕТ.** Карточка переиспользует уже лежащие в
  дереве шрифты (`unbounded-700.woff`), бабочку и зерно; едет обычным rsync.
- **Оффер начинает слать `sendPhoto` вместо `sendMessage`.** Четыре ветки
  fallback (подпись >1024, null-рендер, отказ `sendPhoto`, отказ текста) уводят
  ровно в то сообщение, что уезжало раньше. Ожидаемые новые строки в логе:
  `[rematch] offer photo failed …, falling back to text` и
  `[rematch] search status failed`. Пусто = здорово.
- **Рендер карточки — чистая раскладка + растеризация**, без сети и фотографий,
  кешируется по теме. На путь оффера не добавилось ни одного нового отказа.

Preflight по этому изменению: typecheck чист по 5 проектам, lint чист, **4366
тестов** (бот 3758 / shared 280 / webapp 328, 0 failed; +36 новых, 3 новых
файла). **Шесть гвардов подтверждены красным** до фикса: 10-секундный пол,
повтор глифа, `NEVER_CUT_SHORT`, проглатывание отказа шиммера, гейт по длине
подписи, fallback на null-рендере.

Проверка после деплоя — счастливый путь ничего не логирует, поэтому глазами на
`@gennetytestbot`: временно поднять лимиты через env-оверрайды, дойти до оффера
(должно прийти **фото с подписью и кнопкой**, а не текст), оплатить и засечь
секундомером — шиммер живёт **не меньше 10 с**, меняет четыре бита, стирается,
затем «Нашёл», затем анкета. Отдельно проверить ветку отказа: шиммер обязан
отыграть те же 10 с и только потом отдать возврат. И обе темы получателя.

```sh
pm2 logs gennety-bot --lines 200 --nostream | grep -E "\[rematch\] (offer photo|search status)"
# Пусто = карточка и шиммер уезжают штатно.
psql "$DATABASE_URL" -c "select status, count(*) from rematch_purchases group by 1;"
```

**Rollback:** откатить код и перезапустить. Откатывать больше нечего — ни схемы,
ни состояния; `MESSAGE_EFFECT_REMATCH_ID` становится мёртвым весом.

---

**Deployed 2026-08-20 (was PENDING) — экран выбора пола: два портрета вместо двух строк (PRODUCT_SPEC
§1.3, DECISIONS.md).** **Нет изменения схемы Prisma, нет новых env, нет флагов и
НЕТ ИЗМЕНЕНИЙ СЕРВЕРА ВООБЩЕ** — диф это `apps/webapp/**` плюс документация.
Путь **Deploy Mini App Only** (`./scripts/deploy-webapp.sh`); рсинкать в
`/opt/gennety` нечего, **`pm2 restart` не нужен**. Плюс `pnpm demo:deploy` —
демо собирает свой бандл из того же исходника.

**Четыре вещи, которые стоит знать до редеплоя:**

- **Бандл онбординга тяжелеет на 92 КБ** — два WebP с прозрачностью
  (`male.webp` 51 КБ, `female.webp` 41 КБ). Для сравнения: экран предпочтений на
  том же пути везёт ~530 КБ. В PNG это было бы 762 КБ, поэтому формат тут не
  вкус: `ffmpeg` на этой машине собран без libwebp и `sips` webp не умеет —
  кодирует `@napi-rs/canvas` из `apps/bot`. Ассеты готовит
  `~/Desktop/gennety-gender-avatars/prepare.mjs`, **вне репозитория**, потому
  что деплой синкает рабочее дерево и папка исходников по 2 МБ уехала бы на
  дроплет.
- **Экран пола теперь держится на ~0.6 с дольше**, чтобы проявление цвета не
  обрывалось сменой сцены. Холд стартует ДО запроса, так что реальная добавка —
  `max(0, 600 мс − время сети)`, а не 600 мс сверху. Это единственное место в
  релизе, которое меняет длительность воронки; следить за
  `GET /admin/analytics/onboarding-funnel` на шаге `gender`. Потолок (900 мс)
  закреплён тестом.
- **`saveBasics` получил необязательный `holdMs`**, и его передаёт только экран
  пола. Остальные четыре экрана профиля вызывают его без аргумента и ведут себя
  ровно как раньше — но путь общий, так что регресс тут задел бы всю пятёрку.
- **Подмигивания и искорки в этом релизе НЕТ.** Экран сознательно на половине
  задуманного: они ждут финального арта (DECISIONS.md). Не читать их отсутствие
  как баг.

Preflight по этому изменению: typecheck чист, lint чист, **322 теста вебаппа в
34 файлах** (0 failed, +5 новых), `vite build` чист. Три несущих гварда
подтверждены красными до правок: рассинхрон длительности CSS и константы,
снятый `z-index` на слое арта, убранная маска.

Проверка после редеплоя — экран транзиентный и ничего не логирует, поэтому
глазами, на дев-превью (ни Telegram, ни аккаунт не нужны):

```sh
./scripts/deploy-webapp.sh
pnpm demo:deploy
curl -s https://dating-calendar.gennety.com/onboarding.html \
  | grep -o '/assets/onboarding-[A-Za-z0-9_-]*\.js'
# хеш обязан совпасть с ls apps/webapp/dist/assets/onboarding-*.js
# Дев-превью, обе темы:
#   http://localhost:5173/onboarding.html?preview=basics:gender&lang=ru&theme=dark
# Смотреть на три момента: (1) низ рубашки/волос растворяется в цвете кнопки, а
# не обрывается прямой линией; (2) в покое оба портрета монохромные; (3) по тапу
# цвет приходит у выбранного и НЕ приходит у соседнего, и экран не уезжает
# раньше, чем цвет доехал.
```

**Rollback:** передеплоить Mini App с предыдущего чекаута и оттуда же
`pnpm demo:deploy`. Откатывать больше нечего — ни схемы, ни env, ни флага, ни
серверного состояния.

---

**Deployed 2026-08-20 (was PENDING) — украинский «Думаю…» → «Обмірковую…», единое написание «метч», плюс
два гварда на i18n (DECISIONS.md).** **Нет изменения схемы Prisma, нет новых
env, нет флагов** — но правка задевает `apps/webapp/src/location.ts`, поэтому
релиз **не только серверный**: Deploy Full Server Code → `pnpm db:drift-check`
→ `pm2 restart` → `./scripts/deploy-webapp.sh` → `pnpm demo:deploy`.

Порядок «сервер → Mini App» тут не критичен (стороны независимы и ни одна не
читает данные другой), но **`demo:deploy` пропускать нельзя** — он собирает свой
бандл из того же исходника, и без него демо останется со старым «Думаю…» ровно
на том экране, ради которого всё делалось.

Что меняется у пользователя: в украинском три статусных шиммера («анализирую
AI-память», и два бита Profiler'а между вопросами) и тикер в Location Mini App
говорят «Обмірковую…» вместо «Думаю…»; пять сообщений перестают писать «метч»
тремя разными способами.

**Четыре вещи, которые стоит знать до рестарта:**

- **`ru`/`en`/`de`/`pl` в этих ключах не тронуты вовсе.** Русский по-прежнему
  «Думаю…» — это правильное слово там, и совпадение было проблемой только на
  украинской стороне.
- **Одна из правок видна каждому русскоязычному верифицированному
  пользователю**: `verifyOutcomeVerified` («Напишу, когда найду **мэтч**») —
  первое сообщение после прохождения проверки, единственное место в русском
  блоке, где стояло украинское написание.
- **Появились два гварда в `packages/shared/src/i18n.test.ts`**, и они
  «фейлят закрытым»: следующий ключ, где украинский совпадёт с русским, уронит
  тест, если его не внести в `ALLOWED_IDENTICAL_UK_RU` **с причиной**. Это
  ожидаемое поведение, а не помеха — сообщение теста говорит, что делать.
- **`apps/webapp/src/location-thinking.test.ts` читает `location.ts` как
  ТЕКСТ** (`?raw`), потому что модуль не экспортирует ничего. Переименование
  поля `thinkingSteps` или перевод таблицы на другую форму записи уронит его —
  это цена гварда над файлом без экспортов, и она принята осознанно.

Preflight по этому изменению: typecheck чист по 5 проектам, lint чист,
**4330 тестов** (бот 3722 / shared 280 / webapp 328, 0 failed; +5 новых). Все
три гварда подтверждены красными до правок: два в `i18n.test.ts` (на трёх
«Думаю» и на `matchDeclineDismissed`), один в webapp — откатом строки 747.

Проверка после деплоя — путь ничего не логирует, поэтому глазами на
`@gennetytestbot` или в демо с украинским языком: дождаться вопроса Profiler'а
(между вопросами должно стоять «Обмірковую…» **без эмодзи**) и открыть шаг
«яке місце?» в Location Mini App (в тикере тоже «Обмірковую…»). Косвенно —
что уехал новый бандл:

```sh
./scripts/deploy-webapp.sh
pnpm demo:deploy
curl -s https://dating-calendar.gennety.com/location.html \
  | grep -o '/assets/location-[A-Za-z0-9_-]*\.js'
# хеш обязан совпасть с ls apps/webapp/dist/assets/location-*.js в задеплоенном
# дереве; страница, отдающая 200 со старым хешем, — это старый бандл.
```

**Rollback:** откатить коммит, перезапустить, передеплоить Mini App и демо.
Откатывать больше нечего — ни схемы, ни env, ни флага, ни состояния.

---

**Deployed 2026-08-20 (was PENDING) — песочные часы на карточке истечения: рисованная иконка вместо
примитивов (PRODUCT_SPEC §3.4, DECISIONS.md).** **Нет изменения схемы Prisma,
нет новых env, нет флагов, нет изменения Mini App** (`apps/webapp` не тронут) —
только бот, так что полный деплой кода несёт это целиком, плюс
`pnpm demo:deploy`.

Меняется мотив `expired` целиком плюс один штрих у `penalty` (см. отдельный
блок ниже — стрелка на карточке `penalty` перерисована симметрично). `peer_ignored`
и `missed_date` не тронуты ни на пиксель. Путевые данные `expired` вынесены в
новый модуль
`apps/bot/src/services/expiry-card-hourglass.ts` (~12 кБ), который едет обычным
rsync'ом кода; отдельного ассета нет, файлов в `assets/` не добавлялось.

**Четыре вещи, которые стоит знать до рестарта:**

- **В проде этот путь исполняется редко, но исполняется.** Карточка уходит,
  когда 24-часовое окно решения закрылось без ответа, — это не «ни разу не
  запускалось», как у venue-доски. Проверять всё равно на `@gennetytestbot`
  или в демо: `pnpm --filter @gennety/bot exec tsx ../../scripts/dev-expiry-cards-demo.mjs`
  рендерит и шлёт все четыре варианта в обеих темах, не трогая базу.
- **Знак опережает копию на один такт**, и это осознанно (DECISIONS.md): песок
  всё ещё сыплется, а заголовок говорит «ВРЕМЯ ВЫШЛО». Если после деплоя это
  решат поменять — правка одной функции в новом модуле, без схемы и без env.
- **Рендер по-прежнему без сети и без фотографий** — чистая раскладка +
  растеризация, кэшируется по (variant, theme). Стоимость мотива не изменилась;
  на путь истечения не добавилось ни одного нового отказа.
- **Деградация не тронута:** null-рендер по-прежнему роняет карточку в обычный
  текстовый notice, так что худший случай новой иконки — карточки нет, а не
  свип встал.

Preflight по этому изменению: typecheck чист по 5 проектам, lint чист, **3722
теста бота (241 файл), 0 failed** (+3 новых), три прогона подряд зелёные — в
первом прогоне отвалился один тест, это известный флейк
`date-card/render.test.ts` под параллельной нагрузкой, задокументированный в
блоке 2026-08-02. Все три новых гварда подтверждены красным до фикса: подмена
`bg` на литеральный `white` роняет цветовой тест, а три мутации трансформации
(scale по ширине, смещение, центровка) — геометрический.

Проверка после деплоя — путь ничего не логирует на счастливой ветке, поэтому
глазами:

```sh
pnpm demo:deploy
pm2 logs gennety-bot --lines 200 --nostream | grep -E '\[expiry-card\]|\[expiry-notify\]'
# Пусто у [expiry-card] = ни один рендер не упал. Строки [expiry-notify] —
# обычная телеметрия свипа.
```

**Rollback:** откатить код и перезапустить. Откатывать больше нечего — ни схемы,
ни env, ни флага, ни состояния.

---

**Deployed 2026-08-20 (was PENDING) — стрелка на карточке `penalty` перерисована симметрично
(PRODUCT_SPEC §3.4).** **Нет изменения схемы Prisma, нет новых env, нет флагов,
нет изменения Mini App** — один `<path>` в `expiry-card.ts`, едет тем же
полным деплоем кода, что и блок про песочные часы выше (плюс `pnpm demo:deploy`).

Фаундер заметил на присланном рендере: два «уса» наконечника стрелки визуально
неровные — один почти лежит на линии древка, другой заметно от неё отходит.
Замерено, а не на глаз: угол отклонения от оси древка был **20.5° и 49.8°** —
почти вдвое отличается. Оба уса теперь на **32°** от оси, той же длины (~47px).
Больше ничего на карточке `penalty` не менялось — ни столбики, ни цвет, ни
расположение стрелки.

Preflight: typecheck бота чист, **`expiry-card.test.ts` — 14/14 зелёных**.
Никакой тест не фиксировал старые координаты наконечника — ловить регресс не на
чем, кроме глаз; так и было проверено (`dev-expiry-cards-demo.mjs` на dev-боте).

Проверка после деплоя — то же, что у блока выше: `[expiry-card]` молчит на
счастливом пути, глазами через `pnpm demo:deploy` или
`dev-expiry-cards-demo.mjs`.

**Rollback:** откатить код и перезапустить. Ничего больше не меняется.

---

**Deployed 2026-08-20 (was PENDING) — пользователя предупреждают, что вопросы Profiler'а будут приходить
(PRODUCT_SPEC §1.4 + §Phase 1b, DECISIONS.md).** **Нет изменения схемы Prisma,
нет новых env, нет флагов, нет изменения Mini App** (`apps/webapp` не тронут) —
только бот, так что полный деплой кода несёт это целиком, плюс
`pnpm demo:deploy`.

После пройденной верификации, НАД главным меню, уходит одно короткое сообщение:
пока я ищу человека, буду время от времени задавать простые вопросы; отвечай
честно и не откладывай. До этого первый вопрос Profiler'а прилетал через дни
после регистрации, без единого слова о том, кто спрашивает и зачем.

**Четыре вещи, которые стоит знать до рестарта:**

- **Это +1 сообщение в чате на КАЖДУЮ первую активацию.** Порядок теперь:
  «Проверка пройдена ✨» → предупреждение → меню → закреплённый баннер. На
  рероне (правка фото, админский recheck) не повторяется — держит существующий
  `statusMessageId` guard, тот же, что уже не даёт переслать меню.
- **Копия намеренно НЕ обещает лучший подбор.** Просили сформулировать через
  «более качественное совпадение по интересам»; Profiler по §Phase 1b не входит
  в алгоритм подбора вовсе и кормит только айсбрейкеры и подсказку перед
  свиданием, поэтому обещание переписано на то, что он реально делает. Смысл
  «отвечай честно и быстро → результат лучше» сохранён дословно. Если это
  прочтут как регресс — это решение, а не опечатка (DECISIONS.md).
- **Каденс в тексте не называется** («пока я ищу», не «в течение недели»), потому
  что прод с 2026-08-10 крутит `DROP_CADENCE=daily`.
- **Аккаунт, вошедший через Telegram, но не нажимавший Start, сообщения не
  получает** — проверка `platform in (telegram, both)`, та же, что у
  `workers/profiler.ts`. Соседний `telegramId > 0` для этого недостаточен.

Preflight по этому изменению: typecheck чист по 5 проектам, lint чист, **3723
теста бота (241 файл) + 277 shared, 0 failed** (+4 новых). Оба несущих гварда
подтверждены красным: снятие проверки `platform` и перенос сообщения под меню
роняют по одному тесту.

Проверка после деплоя — путь ничего не логирует на счастливой ветке, поэтому
глазами: пройти верификацию на `@gennetytestbot` (или в демо) и убедиться, что
между «Проверка пройдена ✨» и меню появилось объяснение про вопросы. Косвенно:

```sh
pm2 logs gennety-bot --lines 200 --nostream | grep 'profiler heads-up send failed'
# Пусто = ничего не падает.
```

**Rollback:** откатить код и перезапустить. Откатывать больше нечего — ни схемы,
ни env, ни флага, ни состояния.

---

**Deployed 2026-08-20 (was PENDING) — радиус рынка 60 → 21 км: Киев вместо агломерации (PRODUCT_SPEC §1.3,
DECISIONS.md).** **Нет изменения схемы Prisma, нет новых env, нет флагов** — одно
число в `packages/shared/src/markets.ts`. Но оно и серверное, и клиентское, так
что нужен полный деплой кода **и** редеплой Mini App: Deploy Full Server Code →
`pnpm db:drift-check` → `pm2 restart` → `./scripts/deploy-webapp.sh` →
`pnpm demo:deploy`. Едет в одном релизе с блоком ниже (баннер), у них общий
редеплой вебаппа.

**Порядок: сервер первым.** Клиент получает радиус из
`GET /v1/matches/:id/venue-intent` (`state.market`), то есть старый бандл начнёт
применять новое число сразу после рестарта бота — гейт на клиенте и на сервере
не разъедутся ни на минуту. Обратный порядок дал бы окно, где клиент разрешает
21 км, а сервер ещё принимает 60 (безопасно, но бессмысленно).

**Пять вещей, которые стоит знать до рестарта:**

- **Круг не может отделить город от области, и 21 км — компромисс, а не
  граница.** Замерено от центроида: Вышневое (область) 12.8 км, Пуща-Водица
  (Киев) 14.9 км. То есть Вышневое, Вышгород, Бровары и Ирпень **по-прежнему
  проходят** — отсечь их стоило бы четырёх реальных киевских районов. Уходят
  Борисполь, Буча, Фастов, Обухов, Васильков. Если ожидается «область не
  проходит вообще» — это полигон, отдельная кросс-репная задача.
- **Регистрация из области НЕ закрыта.** Радиус гейтит геолокацию и точку
  выезда; поиск города — отдельный список, так что «Киев» набирается руками из
  любой точки. Цель закрыта наполовину, и это осознанно.
- **Третья ступень venue-лестницы ужалась с 60 до 21 км** (`venue-intent-v2.ts`
  берёт радиус рынка как самый широкий допуск). Продукт больше не готов
  отправить пару за 60 км. Гарантия «пара внутри рынка всегда обслуживается»
  держится при любом радиусе.
- **Существующие аккаунты не трогаются.** `Profile.homeCityKey`/координаты
  канонизируются из записи рынка, а центроид не менялся — переписывать нечего.
  Пользователь из области, зарегистрированный раньше, остаётся с `ua:kyiv` и
  просто не сможет поставить точку выезда у себя дома.
- **iOS не требует правки кода** — он читает `radiusKm` с сервера, так что
  нарисованный `MapCircle` просто станет меньше вместе с гейтом. В iOS-репо
  правится только устаревший комментарий (там было «60 км»).

Preflight по этому изменению: typecheck чист по 5 проектам, lint чист, **3715
тестов бота (240 файлов) + 277 shared + 325 webapp, 0 failed**. Четыре теста
переписаны: они фиксировали ровно старое правило (точка в 35 км обязана
проходить, Бровары обязаны резолвиться в Киев).

```sh
# После рестарта — число, которое видит клиент, читается прямо с сервера:
curl -s https://dating-api.gennety.com/v1/app/config | python3 -m json.tool | grep -A6 supportedCities
# Проверка гейта: в демо дойти до шага точки выезда и оттащить карту в сторону
# Борисполя (~33 км) — Confirm обязан погаснуть. Раньше он оставался живым.
```

**Rollback:** вернуть `radiusKm: 21` → `60`, перезапустить, редеплоить Mini App
и демо. Откатывать больше нечего — ни схемы, ни env, ни флага, ни данных.

---

**Deployed 2026-08-20 (was PENDING) — баннер «точка вне города» перестаёт ломать текст в столбик
(DECISIONS.md).** **Нет изменения схемы Prisma, нет новых env, нет флагов и НЕТ
ИЗМЕНЕНИЙ СЕРВЕРА ВООБЩЕ** — диff это `apps/webapp/**` плюс документация. Путь
**Deploy Mini App Only** (`./scripts/deploy-webapp.sh`); рсинкать в
`/opt/gennety` нечего, **`pm2 restart` не нужен**. Обязательно ещё
`pnpm demo:deploy` — **и он здесь важнее прода**, см. ниже.

Текст в баннере рвался по слову на строку. Причина — флексбокс: демо-кнопка
«Поставить точку в {city}» была прямым ребёнком `display: flex` контейнера, то
есть третьей колонкой, и флекс сжимал предложение, чтобы освободить ей место.
Теперь текст и кнопка в общей колонке (`.market-block-body`).

**Четыре вещи, которые стоит знать до редеплоя:**

- **В проде этот баг НЕ виден, и никогда не был виден.** Кнопка рендерится
  только при `demoMode` (`location.ts` → `marketJumpEl.hidden = !demoMode`), так
  что в проде колонка всегда была одна. Проверять исправление надо в демо-боте;
  в проде проверяется только то, что баннер стал крупнее и короче.
- **Баннер меняется и в проде**: 13 → 14px, приглушённый `--text-dim` → полный
  `--text`, паддинг 11/12 → 13/14. Это ответ на «он маленький», а не побочный
  эффект.
- **Копия короче на одно предложение во всех пяти локалях** — ушло «я ищу место
  недалеко от вас обоих» и повтор `{city}`. Тест на `{city}` в
  `i18n.test.ts` по-прежнему зелёный (плейсхолдер остался, просто один раз).
- **`location.css` добавлен в `test.css.include`** (`vite.config.ts`) — иначе
  `?raw` импорт в новом тесте приходит пустым. Пятый файл в списке; стоимость —
  обработка CSS в тестах только для него.

Preflight по этому изменению: typecheck чист, lint чист, **325 тестов вебаппа в
32 файлах** (0 failed, +5 новых), `vite build` чист. Гвард подтверждён красным:
на старой разметке падает 1 из 5.

```sh
./scripts/deploy-webapp.sh
pnpm demo:deploy
curl -sI https://dating-calendar.gennety.com/location.html | head -1
# Проверка в ДЕМО: дойти до шага выбора точки, оттащить карту за пределы Киева.
# Ожидается: значок, под ним предложение в НОРМАЛЬНУЮ ширину, под предложением
# отдельной строкой «Поставить точку в Киев». Если кнопка снова справа от текста
# и текст в столбик — уехал старый бандл.
```

**Rollback:** передеплоить Mini App с предыдущего чекаута и оттуда же
`pnpm demo:deploy`. Откатывать больше нечего — ни схемы, ни env, ни флага, ни
серверного состояния.

---

**Deployed 2026-08-20 (was PENDING) — с заголовка экрана оплаты билета уходит 🤍 (PRODUCT_SPEC §3.5b,
DECISIONS.md).** **Нет изменения схемы Prisma, нет новых env, нет флагов и НЕТ
ИЗМЕНЕНИЙ СЕРВЕРА ВООБЩЕ** — диff это `apps/webapp/src/ticket/**` плюс
документация. Путь **Deploy Mini App Only** (`./scripts/deploy-webapp.sh`);
рсинкать в `/opt/gennety` нечего, **`pm2 restart` не нужен**. Обязательно ещё
`pnpm demo:deploy` — демо собирает свой бандл из того же исходника, и это
единственное место, где экран сегодня реально проходится.

Пять строк i18n («It's a match» / «Это мэтч» / «Це метч» / «Es ist ein Match» /
«To match») теряют 🤍. Это **снятие явного исключения**, которое PRODUCT_SPEC
держал с 2026-08-08, а не косметика — см. DECISIONS.

**Три вещи, которые стоит знать до редеплоя:**

- **Сервер шлёт своё сердце, и оно остаётся.** Подпись карточки в чате
  (`ticketCardCaption` в `packages/shared/src/i18n.ts`, «Взаимно 🤍 Получи
  *билет на свидание*…») и `matchBothAccepted` не тронуты. То есть после
  деплоя 🤍 будет в чате и не будет внутри Mini App — так и задумано.
- **Ничего, кроме заголовка, не менялось.** `closedTitle`'s 📅 и 🎟️ на кнопках
  на месте; PartnerPaidCard со своим ❤️ не тронут.
- **`TICKET_FEATURE_ENABLED=true` в проде**, но до гейта не дошла ещё ни одна
  пара (0 свиданий за всю историю), так что смотреть надо в демо либо через
  `scripts/dev-stage-all-screens.mjs` на `@gennetytestbot`.

Preflight по этому изменению: typecheck вебаппа чист, **320 тестов в 31 файле**
(0 failed, +1 новый). Гвард подтверждён красным до правки.

```sh
./scripts/deploy-webapp.sh
pnpm demo:deploy
curl -sI https://dating-calendar.gennety.com/ticket.html | head -1
# Затем глазами: открыть экран билета и убедиться, что заголовок «Это мэтч» —
# без сердечка, а карточка билета под ним не изменилась.
```

**Rollback:** передеплоить Mini App с предыдущего чекаута и оттуда же
`pnpm demo:deploy`. Откатывать больше нечего — ни схемы, ни env, ни флага, ни
серверного состояния.

---

**Deployed 2026-08-20 (was PENDING) — вступление демо встаёт НАД первым вопросом чата (DEMO_MODE.md,
DECISIONS.md).** **Только демо** — диff это `apps/bot/src/demo/**` плюс
документация, так что **в `/opt/gennety` синкать нечего и прод НЕ
перезапускается**; весь деплой — `pnpm demo:deploy`. Нет изменения схемы, нет
env, нет флагов, нет изменения Mini App.

Beat `intro` ждал передачи чата из мини-аппа, а `/complete` зовёт
`runAgentTurn` + `sendMarkdownSafe` **синхронно внутри своего запроса** — то
есть «Чем тебе нравится заниматься?» уже в чате к моменту, когда драйвер
просыпается (тик 3с). Визитёр читал вопрос анкеты ВЫШЕ сообщения о том, что это
вообще демо. Теперь триггер — известный `User.language`, который пишет первый же
экран мини-аппа.

**Три вещи, которые стоит знать до редеплоя:**

- **Сообщение приходит, пока визитёр внутри полноэкранного мини-аппа** — за
  минуты до передачи чата (впереди визуальное интро, контактный гейт, город,
  тема, пять экранов профиля). Это ожидаемо: оно читается, когда мини-апп
  закрывают, ровно над вопросом. Побочный эффект — уведомление Telegram во время
  прохождения мини-аппа.
- **Окно beat'а стало шире** — [выбор языка, активация) вместо [передача чата,
  активация). `spokenBeats` живут в памяти (демо-схемы нет), так что рестарт
  демо ВНУТРИ мини-аппа теперь может повторить вступление; раньше эта фаза в
  окно не попадала. Тот же принятый размен, на несколько минут длиннее.
- **Прод не затронут вообще** — ни одного файла вне `apps/bot/src/demo/`.
  Проверяется счётчиком: у `gennety-bot` число рестартов обязано остаться
  прежним.

Preflight: typecheck бота чист, **106 тестов `src/demo/` зелёные** (+1 новый).
Гвард подтверждён красным до фикса — на прежнем условии новый тест падает.

Проверка после деплоя — путь ничего не логирует, поэтому глазами в демо-боте:
`/restart`, затем `/start`, пройти мини-апп до конца и убедиться, что в чате
сначала «Это демо-режим Gennety 👋», а уже под ним первый вопрос.

```sh
pnpm demo:deploy
ssh root@167.172.178.229 'pm2 describe gennety-bot | grep -E "restarts"'
# Число рестартов прода не изменилось = демо-деплой не задел его.
```

**Rollback:** откатить код и `pnpm demo:deploy`. Откатывать больше нечего — ни
схемы, ни env, ни флага; прод не затронут ни в одну сторону.

---

**Deployed 2026-08-20 (was PENDING) — конфетти на экране согласованного времени перестают прилипать к
верху экрана (DECISIONS.md).** **Нет изменения схемы Prisma, нет новых env, нет
флагов и НЕТ ИЗМЕНЕНИЙ СЕРВЕРА ВООБЩЕ** — диff это `apps/webapp/**` плюс
документация. Путь **Deploy Mini App Only** (`./scripts/deploy-webapp.sh`);
рсинкать в `/opt/gennety` нечего, **`pm2 restart` не нужен**. Обязательно ещё
`pnpm demo:deploy` — демо собирает свой бандл из того же исходника.

Частицы стартуют со своей задержкой (до 1.5с), а при дефолтном
`animation-fill-mode: none` элемент во время задержки рисуется своим стилем —
без трансформа, то есть на y=0, непрозрачным, вплотную к верхней кромке.
Замерено на живом рендере (headless Chromium 390x844, анимации заморожены и
прошагены через WAAPI): **40 из 40 стоят на кромке в t=0, 33 на 400мс, 21 на
800мс**. Стало `... infinite backwards` — задержка проходит на кейфрейме 0%
(`translateY(-10vh)`, выше экрана).

**Три вещи, которые стоит знать до редеплоя:**

- **Правка обязана быть в JS, а не в CSS**, и это единственная нетривиальная
  часть: `piece.style.animation` — инлайновый shorthand, он сбрасывает
  fill-mode в `none` что бы ни говорил стайлшит. CSS-версия этой же правки не
  сдвинула ни одного замеренного пикселя. Держится тестом
  `confetti-fill.test.ts`, подтверждённым красным.
- **Первые ~0.4с экран теперь пустее, чем был** — раньше приклеенные частицы
  давали что-то сразу. Это и есть исправление: кадр наполняется за первую
  секунду задержек (замерено: 5 видимых на 400мс, 16 на 800мс). Отрицательные
  задержки (как у падающих денег в интро) сознательно НЕ применялись — см.
  DECISIONS.md.
- **Само падение не тронуто**: число реально падающих частиц на каждом срезе
  совпадает до и после (2 / 12 / 28), и под `prefers-reduced-motion` конфетти
  как не было, так и нет (`runConfetti` выходит раньше).

Preflight по этому изменению: typecheck чист, lint чист, **319 тестов вебаппа в
31 файле** (0 failed, +2 новых), `vite build` чист.

Проверка после редеплоя — экран транзиентный и ничего не логирует, поэтому
глазами. Нужна пара в `negotiating` с открытым календарём, чего в проде пока не
случалось (0 свиданий за всю историю), так что это `@gennetytestbot`
(`scripts/dev-calendar-solo-demo.mjs`, запускать с `TZ=Europe/Kyiv`, предварительно
убрав существующий матч пары — пожизненный бан не даст спариться заново) либо
демо-бот:

```sh
./scripts/deploy-webapp.sh
pnpm demo:deploy
curl -sI https://dating-calendar.gennety.com/ | head -1
# Открыть календарь, выбрать пересекающийся слот и смотреть на первую секунду:
# ни одной частицы не должно стоять неподвижно на верхней кромке — они входят
# сверху уже в движении. Если ряд квадратиков «висит» вверху — уехал старый
# бандл (правка целиком в JS, так что кэш CSS тут ни при чём).
```

**Rollback:** передеплоить Mini App с предыдущего чекаута и оттуда же
`pnpm demo:deploy`. Откатывать больше нечего — ни схемы, ни env, ни флага, ни
серверного состояния.

---

**Deployed 2026-08-20 (was PENDING) — кнопка «Продолжить» перестаёт прыгать над клавиатурой
(PRODUCT_SPEC §1.1, DECISIONS.md).** **Нет изменения схемы Prisma, нет новых
env, нет флагов и НЕТ ИЗМЕНЕНИЙ СЕРВЕРА ВООБЩЕ** — диff это `apps/webapp/**`
плюс документация. Путь **Deploy Mini App Only**
(`./scripts/deploy-webapp.sh`); рсинкать в `/opt/gennety` нечего, **`pm2 restart`
не нужен**. Обязательно ещё `pnpm demo:deploy` — демо собирает свой бандл из
того же исходника и иначе останется на старом.

Экран ввода имени швырял кнопку вверх при появлении клавиатуры и вниз при её
уходе. Дело не в верстке, а в кривой: `.ob-basics` анимировал высоту
`cubic-bezier(0.22, 1, 0.36, 1)` за 200мс, а эта кривая стартует на **4.55×**
собственной средней скорости (наклон = 1 / 0.22). Замерено на живом рендере,
а не выведено: **один кадр уносил 115px из 336** — треть пути за 16мс, дальше
80мс ползком через последние 7%. Стало `cubic-bezier(0.42, 0, 0.58, 1)` за
260мс, худший кадр **38px (11.3%)**.

**Четыре вещи, которые стоит знать до редеплоя:**

- **Меняются ТРИ поверхности, а не одна.** Общий токен `--kb-ease` теперь
  обслуживает экраны профиля (`.ob-basics`), карточку гейтов (`.gate-main` —
  почта / телефон / город) и `max-height` списка внутри неё (`.gate-card`).
  Последний раньше не анимировался вовсе, поэтому на экране города список
  прыгал за кадр до того, как карточка вокруг него доезжала. Гейты заодно
  становятся на 60мс длиннее (было `200ms ease`).
- **Проверять надо на телефоне, а не в дев-превью.** В headless-браузере нет
  экранной клавиатуры: `--kb-height` там всегда 0, и увидеть эффект можно
  только выставив переменную руками (команда ниже). Реальная проверка — пройти
  онбординг на `@gennetytestbot` (нужен HTTPS-туннель и `WEBAPP_URL` на него)
  или в демо-боте.
- **На Android эффекта может не быть, и это не регресс.** Там WebView сжимает
  сам layout viewport, `--kb-height` остаётся 0, и анимацией владеет браузер —
  наш transition в этом случае не участвует вовсе. Правка бьёт по iOS-пути,
  где клавиатура «плавает» поверх страницы.
- **Записи `--kb-height` теперь склеиваются в один кадр** и игнорируются ниже
  2px. Это страховка от того, что поток событий `visualViewport` перезапускает
  transition по нескольку раз за кадр; поведение при этом не меняется, потому
  что 2px на 336px клавиатуре невидимы.

Preflight по этому изменению: typecheck чист, lint чист, **317 тестов вебаппа
в 30 файлах** (0 failed, +4 новых), `vite build` чист. Все три гварда
подтверждены красным: старая кривая роняет два теста (4.545× и 34.26% — сходится
с браузерным замером 34.2%), захардкоженная кривая на одном правиле роняет
третий.

```sh
./scripts/deploy-webapp.sh
pnpm demo:deploy
curl -sI https://dating-calendar.gennety.com/onboarding.html | head -1
# Дев-превью (клавиатуры нет, поэтому переменная выставляется руками):
#   http://localhost:5173/onboarding.html?preview=basics:name&lang=ru&theme=dark
#   в консоли: document.documentElement.style.setProperty('--kb-height','336px')
# Смотреть на два момента: (1) кнопка не «телепортируется» первым кадром, а
# разгоняется и тормозит; (2) при возврате к 0 падает так же, а не рывком.
# На телефоне: тапнуть по полю имени и убедиться, что подъём читается как одно
# движение. Затем экран города — список и карточка должны ехать вместе.
```

**Rollback:** передеплоить Mini App с предыдущего чекаута и оттуда же
`pnpm demo:deploy`. Откатывать больше нечего — ни схемы, ни env, ни флага, ни
серверного состояния.

---

**Deployed 2026-08-20 (was PENDING) — доска смены места: 21 карточка вместо 12 (PRODUCT_SPEC §3.7b,
DECISIONS.md).** **Нет изменения схемы Prisma, нет новых env, нет флагов, нет
изменения Mini App** (`apps/webapp` не тронут — список рендерится по
`items.length`, фиксированной сетки нет) — только бот, так что полный деплой
кода несёт всё целиком, плюс `pnpm demo:deploy`.

Четыре константы в `services/venue-change.ts`: `VENUE_CHANGE_CATALOG_LIMIT`
12 → 21, `VENUE_CHANGE_WALK_RESERVED` 3 → 5, `VENUE_CHANGE_PREMIUM_PINNED`
3 → 5, `VENUE_CHANGE_PREMIUM_MAX` 5 → 8. Резервы двигаются вместе с потолком не
за компанию: при голом расширении симуляция по всем киевским центрам оставляет
среднее число прогулочных мест на **~2.6**, то есть девять новых слотов уходят
целиком в кафе — см. DECISIONS.

**Четыре вещи, которые стоит знать до рестарта:**

- **Расход на Google Places на открытие доски растёт с 12 до 21 запроса.**
  Curated-строки не хранят фотографий, так что резолв — один Place Details на
  карточку. Кэш в памяти на сутки по `placeId`, так что платится это на холодном
  процессе; после рестарта первые открытия доски будут дороже обычного. Плюс до
  21 обращения к фото-прокси на плитки вместо 12 — ретраи там уже есть
  (2026-08-08), но доля затронутых плиток при блипе выше.
- **Ожидается, что пар будет чаще выкидывать в состояние «оба лайкнули, ничего
  не совпало».** Это осознанная цена (§3.7b), а не регресс: разнообразие
  оплачивается кругами согласования. Если начнёт мешать — смотреть надо на
  потолок, а не на резервы.
- **В проде это ещё нечем исполнить.** До этой доски не дошла ни одна пара (0
  свиданий за всю историю), а `VENUE_CHANGE_FEATURE_ENABLED` гейтит саму кнопку.
  Проверять в демо (там доска реально открывается каждый прогон) или на
  `@gennetytestbot`. Дев-превью Mini App показывает свой мок-каталог и этого
  правила НЕ покажет.
- **Тесты переведены с литерала 12 на константу** — следующее движение потолка
  их уже не потребует.

Preflight по этому изменению: typecheck бота чист, **63 теста
`venue-change.test.ts` зелёные**. Гвард подтверждён красным: занейтраленный
резерв прогулочных мест роняет 3 теста.

Проверка после деплоя — путь ничего не логирует на счастливой ветке, поэтому
глазами в демо: дойти до назначенного свидания, открыть «Сменить место» и
убедиться, что карточек около двух десятков, сверху пять премиальных, а среди
остальных есть парки/набережные, а не только столики.

```sh
pnpm demo:deploy
ssh root@167.172.178.229 'pm2 logs gennety-bot --lines 200 --nostream | grep "\[venue-change\] photo"'
# Всплеск "failed after 3 attempt(s)" = 21 запрос на доску давит на Places
# сильнее, чем 12; тогда смотреть на квоту, а не на ретраи.
```

**Rollback:** вернуть четыре константы и перезапустить. Откатывать больше нечего
— ни схемы, ни env, ни флага, ни состояния.

---

**Deployed 2026-08-20 (was PENDING) — марка успеха: логотип раскручивается и уходит, остаётся галочка
(PRODUCT_SPEC → Cross-Cutting Concerns, DECISIONS.md).** **Нет изменения схемы
Prisma, нет новых env, нет флагов и НЕТ ИЗМЕНЕНИЙ СЕРВЕРА ВООБЩЕ** — диff это
`apps/webapp/**` плюс документация. То есть путь **Deploy Mini App Only**
(`./scripts/deploy-webapp.sh`); рсинкать в `/opt/gennety` нечего, **`pm2 restart`
не нужен**. Обязательно ещё `pnpm demo:deploy` — демо собирает свой бандл из того
же исходника и иначе останется на старом.

Четвёртая версия общей марки успеха на пяти экранах (verification, Type Radar,
календарь, venue-change, финал онбординга). Было: бабочка прилетает с пружиной и
остаётся. Стало: бабочка стоит, делает взмах вправо, раскручивается влево с
ускорением, уменьшается в точку — и прочерчивается галочка.

**Пять вещей, которые стоит знать до редеплоя:**

- **Два самозакрывающихся экрана закрываются на ~600мс ПОЗЖЕ.**
  `SUCCESS_TOTAL_MS` 700 → **1300**, `SUCCESS_ARRIVE_MS` 520 → **1180**;
  `SUCCESS_READ_MS` намеренно не тронут. Итого verification и Type Radar держат
  экран 2400мс вместо 1800 — примерно там же, где была версия с чертящейся
  галочкой (2300мс). Это реальное время в воронке: смотреть на
  `GET /admin/analytics/onboarding-funnel`. Потолок закреплён тестом.
- **Коробка марки выросла 190×138.8 → 190×190px** (кадр стал квадратным: она
  теперь ВРАЩАЕТСЯ, и запас нужен по обеим осям). Это +51px по вертикали на пяти
  экранах. Проверено глазами на verification и venue-change в обеих темах — не
  поджимает; остальные три того же типа (центрированная колонка с заголовком
  под маркой).
- **Замерено, а не выведено:** худший запас крыла до края кадра **4.3px** на
  460мс, коробка ровно 190×190. Крыло нигде не срезается.
- **Тронут `brand-butterfly.ts`? Нет.** Силуэт и градиент логотипа не менялись,
  так что **лоадер не затронут** — в отличие от релиза 08-15.
- **Появился dev-only ревью-маршрут**, `import.meta.env.DEV`-гейт, в
  продакшн-бандле его нет: на `verification.html?screen=success` тап по экрану
  переигрывает анимацию, а `&mark=tick` показывает вариант без бабочки. Удалить
  вместе с блоком, как только выбор сделан.

Preflight по этому изменению: typecheck чист, lint чист, **313 тестов вебаппа в
29 файлах** (0 failed), из них 27 на саму марку. **Шесть гвардов подтверждены
красным** до фикса: тесный кадр, равномерная раскрутка, отсутствие взмаха,
линейный взмах, бабочка оставшаяся в кадре покоя, галочка поставленная в
очередь вместо наложения.

Проверка после редеплоя — экраны транзиентные и ничего не логируют, поэтому
глазами:

```sh
./scripts/deploy-webapp.sh
pnpm demo:deploy
curl -sI https://dating-calendar.gennety.com/verification.html | head -1
# Дев-превью, обе темы (тап по экрану = переиграть):
#   http://localhost:5173/verification.html?screen=success&lang=ru&theme=dark
#   http://localhost:5173/verification.html?screen=success&lang=ru&theme=light
# Смотреть на четыре момента:
#   (1) логотип СТОИТ, потом взмах ВПРАВО — если сразу поехал влево, уехал
#       старый бандл или сломана расстановка кейфреймов;
#   (2) раскрутка ускоряется, а не идёт равномерно;
#   (3) крыло нигде не срезается краем кадра на первой четверти оборота;
#   (4) в покое — только галочка, на тёмной теме заметно светлее бордового.
```

**Rollback:** передеплоить Mini App с предыдущего чекаута и оттуда же
`pnpm demo:deploy`. Откатывать больше нечего — ни схемы, ни env, ни флага, ни
серверного состояния.

---

**Deployed 2026-08-20 (was PENDING) — демо-партнёр предлагает вечер вместо 13:00 (DEMO_MODE.md,
DECISIONS.md).** **Только демо** — диff это `apps/bot/src/demo/**` плюс
документация, так что **в `/opt/gennety` синкать нечего и прод НЕ
перезапускается**; весь деплой — `pnpm demo:deploy`. Нет изменения схемы, нет
env, нет флагов, нет изменения Mini App.

Кукла в календаре встречно предлагала **13:00 на каждом демо, всегда**: сетка
приходит по дням и по возрастанию времени (13:00→19:30 Киев), а
`pickCounterSlots` брала первый свободный слот дня. Теперь целится в вечер, по
одному слоту на день, с чередованием часа 18:00 / 19:00 / 17:00.

**Три вещи, которые стоит знать до редеплоя:**

- **Прод не затронут вообще** — ни одного файла вне `apps/bot/src/demo/`.
  Проверяется счётчиком рестартов `gennety-bot`: он обязан остаться прежним.
- **Час вечера считается в `Europe/Kyiv`**, взятой из `DEFAULT_TIME_ZONE`
  (`@gennety/shared`), которая зеркалит `CALENDAR_TIME_ZONE` в
  `handlers/matching/scheduler.ts`. Импортировать сам scheduler нельзя — он
  тянет Prisma в модуль, который обязан тестироваться без базы. Если продукт
  когда-нибудь начнёт назначать свидания вне Киева, эти две константы разъедутся.
- **Второй проход больше не складывает три слота в один день** (визитёр
  отметил что-то в каждом дне). Поведение стало соответствовать докстроке
  функции, а не наоборот.

Preflight: typecheck чист, lint чист, **полный набор бота 3713 тестов / 240
файлов, 0 failed** (+5 новых). Гвард подтверждён красным до фикса — 4 из 5
новых тестов падают на старой реализации.

Проверка после деплоя — путь ничего не логирует, поэтому глазами в демо-боте:
дойти до календаря, отметить любое время и дождаться встречного предложения —
все предложенные слоты должны быть 17:00 или позже, на разных днях.

```sh
pnpm demo:deploy
ssh root@167.172.178.229 'pm2 describe gennety-bot | grep -E "restarts"'
# Число рестартов прода не изменилось = демо-деплой не задел его.
```

**Rollback:** откатить код и `pnpm demo:deploy`. Откатывать больше нечего — ни
схемы, ни env, ни флага; прод не затронут ни в одну сторону.

---

**Deployed 2026-08-15 — релиз из 25 коммитов: все 10 PENDING-блоков разом
(`cd25c56`), плюс production Mini App и вкладка Core metrics в дашборде.**
Полный деплой кода + аддитивный `db:push` + Mini App + демо + отдельный
редеплой дашборда. Прод поднят с `bcb82c9` до `cd25c56`.

Порядок был: worktree → preflight → schema diff → rsync → install/build →
`db:push` → `db:drift-check` → `pm2 restart` → `deploy-webapp.sh` →
`demo:deploy` → пуш дашборда. Дашборд **последним** намеренно: он читает
`conversion`/`genderRatio`, и обратный порядок дал бы сломанную вкладку —
то же правило, на котором обжёгся блок account-health 2026-08-07.

**Схема:** ровно 4 `ADD COLUMN` (`matches.date_attended_a/_b`,
`attendance_outcome_a/_b`), **ноль DROP** — план прочитан `migrate diff` до
запуска. `db:drift-check` **OK**, колонки подтверждены запросом к
`information_schema` до рестарта.

**⚠️ `security:audit` снова упал на preflight — третий релиз подряд, и снова
на протухшем оверрайде.** `nanoid` стоял на `3.3.17`, а advisory
GHSA-2v37-7h3g-55p8 расширился до `<3.3.18`. Путь сборочный
(`apps/video > @remotion/cli > … > postcss > nanoid`), рантайма бота не
касается, но гейт pass/fail. Поднято до `3.3.18` (`cd25c56`), переаудит
чистый. **Это уже не инцидент, а постоянный налог** — перечитывать пины
оверрайдов каждый деплой, а не только когда добавляешь новый.

**⚠️ Production Mini App едва не остался позади.** Бэкенд-деплой и
`demo:deploy` прошли, а `deploy-webapp.sh` — нет, и три блока онбординга
(марка успеха, падающие деньги, экран предпочтений) лежали бы на дроплете в
исходниках при старом собранном бандле. Поймано сравнением хешей:
прод отдавал `onboarding-y-xXMY4C.js` против собранного `onboarding-5tbpPWp0.js`,
и `grep` по бандлу давал ноль вхождений новой марки. **`demo:deploy` не
заменяет `deploy-webapp.sh`** — он собирает СВОЙ бандл в `/var/www/demo-app`
и лишь возвращает `dist/` к прод-URL. После деплоя, который трогал
`apps/webapp`, сверяй хеш ассета, а не факт «страница отдаёт 200».

Preflight в изолированном worktree (в рабочем дереве шла параллельная правка
венью-каталога): typecheck чист по 5 проектам, lint чист, **4289 тестов**
(бот 3708 / shared 276 / webapp 305, 0 failed), `pnpm build`,
`security:secrets` (1078 файлов), `security:audit` 0 advisories после фикса.

rsync dry-run дал **176 удалений**, все прочитаны: артефакты Remotion
(`apps/video/out`, включая вложенный дубль `apps/video/apps/video/out`) плюс
`.DS_Store`. Оба бэкапа БД, оба ключа `keys/*.p8` и 16 снапшотов `.env.bak.*`
проверены на месте ПОСЛЕ синка. Worktree пишет `.git` файлом, а
`--exclude '.git/'` ловит только каталоги — указатель удалён вручную с
дроплета, как предписывает секция «Prod anchor».

**Проверено на живом проде (замерено, а не выведено):**

- `/admin/stats` отдаёт оба новых блока: `conversion` с `netPct: null`,
  `excludedSynthetic: 13`, `excludedTest: 1`; `genderRatio` 6 M / 1 F / 16
  unknown из 23 (**69.6% не назвали пол**).
- `/admin/dashboard.derived` — все 11 полей; `weeklyPaidDates: 0`,
  `registeredToMatchRate7dPct: 0` при 4 реальных регистрациях,
  `cacPerPaying`/`ltvCac`/`roas` — `null`.
- `/admin/matches` — 9 выведенных полей на строке (`noShow: null`,
  `attendance: "unknown"` — «не знаем», не «не встретились»).
- Все 26 полей, которые читает новая вкладка дашборда, существуют в живом
  ответе — сверено программно, потому что типы фронта написаны руками и TS
  расхождение имён не поймает.
- Компонент отрендерен в строку на живом продовом JSON: 13.7 кБ HTML, без
  исключений. Тот же класс дефекта, что чинил `04d142d` в репозитории
  дашборда.
- Бот: `Bot @gennetybot started`, все 16 кронов + peer-wait воркер, рестарты
  62 → **63**, `unstable restarts: 0`, **ноль ошибок от нового PID**, ноль
  `P2022`/`P2023`/`ERR_MODULE_NOT_FOUND`. `/v1/ping` ok, admin `401`, **все 11
  страниц Mini App 200**.
- Демо переехало тем же исходником, изоляция подтверждена его баннером:
  `@gennety_demo_bot`, база `aws-1-eu-west-1` (у прода `aws-0-`), оба
  демо-подавления кронов на месте, рестарты 24 → 25. **Прод при этом не
  тронут** — счётчик рестартов держался на 63.

**Что этот релиз НЕ включает, и это решения, а не пробелы:** CAC/LTV/ROAS
остаются `null` до реализации `AD_SPEND_TRACKING_DESIGN.md` (Задача 3 была
«только дизайн»); явка спрашивается только в Telegram, у app-пользователей
`attendance` остаётся `unknown`; билет при no-show не возвращается.

**Rollback:** пересинкать чекаут на `bcb82c9`, перезапустить, оттуда же
редеплоить Mini App и демо, и откатить `d6d8462` в репозитории дашборда.
Аддитивные колонки могут остаться — старый код их не читает.

---

**Deployed 2026-08-15 (was PENDING) — метрика Match→Ticket, Core metrics, поля матча (ТЗ Задачи 1–2,
DECISIONS.md).** **Нет изменения схемы Prisma, нет новых env, нет флагов, нет
изменения Mini App** — только админ-API. **Но едет в одном релизе с блоком ниже
(«вы встретились?»), которому `db:push` НУЖЕН**: `/admin/stats` читает
`matches.date_attended_a`, так что без пуша схемы этот эндпоинт отдаст 500.
Порядок: Deploy Full Server Code → `db:push` → `pnpm db:drift-check` →
`pm2 restart` → редеплой дашборда (отдельный репозиторий).

Проверено на живых данных до деплоя (сырым SQL, потому что колонок в проде ещё
нет — пробник воспроизвёл ровно тот `P2022`, о котором предупреждает блок ниже):

```
conversion:      confirmed 0, netPct null, excludedSynthetic 13, excludedTest 1
weeklyPaidDates: 0
genderRatio:     6 M / 1 F / 16 unknown из 23 → 69.6% не назвали пол
reg→match 7d:    4 реальных регистрации, 0 матчей
```

**Пять вещей, которые стоит знать до рестарта:**

- **`netPct: null`, а не 0%, и это правильный ответ.** Знаменатель пуст: 13
  матчей синтетические (партнёр-заглушка по построению всегда отказывает и
  билет купить не может), а единственный `completed` в истории прода оказался
  засеянной строкой с синтетическим участником. Раздел будет пустым, пока не
  появятся продажи.
- **`/admin/stats` и `/admin/matches` получают по одному новому запросу**
  (`ticketLedger.groupBy` по возвратам). На `/admin/matches` он ограничен
  строками страницы, не N+1. Семантика отказа та же, что у остальных девяти
  запросов `collectStats`: падение любого — 500 на весь эндпоинт.
- **`derived.activeRate` и соседи не тронуты**, но `derived` вырос на 11 полей.
  Дашборд читает их через `.catch(() => null)` по секциям, так что старый фронт
  не сломается — просто не покажет новое.
- **`HEALTH_USER_SELECT` получил колонку `gender`.** Ни одно правило здоровья её
  не читает; она нужна разрезу Gender Ratio, которому требуется тот же вердикт
  «тестовый / реальный». Второй скан пользователей ради одной колонки был бы
  платой за дубль.
- **CAC/LTV/ROAS отдаются `null` намеренно.** Данных о расходах в продукте нет
  вовсе; дизайн ввода — `AD_SPEND_TRACKING_DESIGN.md`, реализация не начата.
  Показывать 0 вместо «нет данных» — единственная ошибка в этом разделе,
  которая делает дашборд хуже, чем его отсутствие.

Preflight: typecheck чист по всем 5 проектам, lint чист, **3704 теста бота, 0
failed** (+33 новых).

Проверка после деплоя:

```sh
KEY=$(ssh root@167.172.178.229 "sed -n 's/^ADMIN_API_KEY=//p' /opt/gennety/.env | tail -1 | tr -d '\"'")
curl -s -H "Authorization: Bearer $KEY" https://api-admin.gennety.com/admin/stats \
  | python3 -c "import json,sys; d=json.load(sys.stdin); print(d['conversion']); print(d['genderRatio'])"
# conversion.netPct должен быть null (не 0), excludedSynthetic > 0.
curl -s -H "Authorization: Bearer $KEY" 'https://api-admin.gennety.com/admin/matches?limit=1' \
  | python3 -c "import json,sys; print(json.load(sys.stdin)['data'][0])"
# У строки должны быть noShow, attendance, refundReason, dateCompletedAt.
```

**Rollback:** откатить код и перезапустить. Откатывать больше нечего — своей
схемы этот блок не добавляет, env не трогает, флага нет.

---

**Deployed 2026-08-15 (was PENDING) — демо перестаёт слать финал после каждого рестарта (DEMO_MODE.md,
DECISIONS.md).** **Только демо** — диff это `apps/bot/src/demo/**` плюс
документация, так что **в `/opt/gennety` синкать нечего и прод НЕ
перезапускается**; весь деплой — `pnpm demo:deploy`. Нет изменения схемы, нет
env, нет флагов, нет изменения Mini App.

Закрывающее сообщение («Ось і весь продукт — від реєстрації до відгуку після
побачення») уходило заново примерно через 12 с после **каждого** рестарта
`gennety-demo`, бесконечно, пока визитёр не нажмёт кнопку или `/restart`.
Защита от повтора (`redoOffered`) живёт в памяти процесса, а матч в статусе
`completed` остаётся терминальным навсегда — то есть окно не закрывалось ничем.
Теперь финал предлагается только если матч завершился в последние 10 минут.

**Три вещи, которые стоит знать до редеплоя:**

- **Замерено в демо-базе, а не выведено.** Ровно два таких сообщения, оба одному
  визитёру про один матч (`completed` с 15:19:12): настоящее в 15:19:29 и
  повторное в 19:21:24 — процесс стартовал в 19:20:57, то есть через 27 с после
  рестарта. Всего 24 рестарта; память 45 МБ при лимите 300 МБ, так что это
  деплои, а не memory-restart'ы.
- **Остаточный случай оставлен намеренно:** рестарт внутри 10-минутного окна,
  уже после отправки, всё ещё повторит сообщение. Цена нулевая — кнопка на
  исходном сообщении работает после любых рестартов (её хендлер читает только
  БД), так что второй заход по флоу доступен всегда.
- **Прод не трогается вообще.** Проверить это проще всего по счётчику: у
  `gennety-bot` число рестартов после деплоя обязано остаться прежним.

Preflight: typecheck демо-файлов чист, **100 тестов `src/demo/` зелёные** (+6
новых). Гвард подтверждён красным до фикса — снятие проверки возраста роняет 1
тест из 6. *(В общем прогоне 3 файла падают — `admin/routes/ops.test.ts`,
`user-health.test.ts` и их утилиты: это незакоммиченная работа параллельной
сессии в общем дереве, к демо отношения не имеет. Дождаться её или деплоить из
изолированного `git worktree`.)*

Проверка после деплоя — путь ничего не логирует на счастливой ветке, поэтому по
базе и по счётчику рестартов:

```sh
pnpm demo:deploy
ssh root@167.172.178.229 'pm2 describe gennety-bot | grep -E "restarts"'
# Число рестартов прода не изменилось = демо-деплой не задел его.
# Затем: перезапустить демо ещё раз и убедиться, что финал НЕ приходит.
ssh root@167.172.178.229 'pm2 restart gennety-demo && sleep 45 && pm2 logs gennety-demo --lines 40 --nostream'
```

**Rollback:** откатить код и `pnpm demo:deploy`. Откатывать больше нечего — ни
схемы, ни env, ни флага; прод не затронут ни в одну сторону.

---

**Deployed 2026-08-15 (was PENDING) — «вы встретились?» на T+24h (PRODUCT_SPEC §Phase 4, DECISIONS.md).**
**Нет новых env, нет флагов, нет изменения Mini App** (`apps/webapp` не тронут) —
только бот, **но нужен аддитивный `db:push` ДО рестарта**, плюс
`pnpm demo:deploy` после.

Четыре nullable-колонки (`matches.date_attended_a/_b`,
`attendance_outcome_a/_b`) читаются и пишутся хендлером ответа, так что база без
них бросит `P2022` на первом же тапе — тот самый PM2 crash-loop, о котором
предупреждает этот файл. Сначала убедиться, что план аддитивный (ожидается
четыре `ADD COLUMN`, ноль `DROP`):

```sh
export DATABASE_URL="$(sed -n 's/^DATABASE_URL=//p' .env | tail -1 | tr -d '"')"
pnpm --filter @gennety/db exec prisma migrate diff \
  --from-schema-datasource prisma/schema.prisma \
  --to-schema-datamodel prisma/schema.prisma --script
pnpm --filter @gennety/db db:push
pnpm db:drift-check   # должен вернуть 0 до pm2 restart
```

Что меняется для пользователя: DM на T+24h больше **не** форма обратной связи, а
вопрос «вы встретились?» с двумя кнопками. Форма приходит следом, на ответ «да».
До этого продукт не знал, состоялось ли свидание, вообще ничем:
`status='completed'` ставится по таймеру, а форма спрашивает химию 1–10 — вопрос
про свидание, которого могло не быть.

**Пять вещей, которые стоит знать до рестарта:**

- **Появляется новый вызов OpenAI, но почти никогда.** Улики (что человек писал
  агенту после свидания + прокси-чат) читаются перед вопросом, и модель
  вызывается ТОЛЬКО если улики есть. У пары без них — ноль вызовов. Модель
  выбирает формулировку и никогда не пишет факт явки.
- **Читатель улик fail-open, и это проверено тестом.** Любая ошибка чтения
  деградирует до нейтрального вопроса — вопрос обязан уйти в любом случае.
  В логах это `[attendance] evidence read failed`.
- **Мобильная рельса не меняется вообще.** Push продолжает слать прежнее
  приглашение в форму, `/v1/me/feedback/pending` не тронут. То есть у
  app-пользователей регрессии нет, но и про явку их не спрашивают — это
  отдельная задача с добавлением в `/v1/*`.
- **`SessionData` получает состояние `awaiting_attendance`** с 24-часовым
  дедлайном. Аддитивно, без изменения схемы (`bot_sessions` хранит blob), и
  fail closed: сессия, записанная до деплоя, читает `null` и не считается живой.
- **Ничего не exercised в проде сразу.** Нужна пара, дошедшая до `scheduled`, а
  за всю историю прода органических свиданий не было ни одного (единственный
  `completed` — засеянная строка с `agreedTime` раньше `createdAt`). Проверять
  на `@gennetytestbot` или в демо.

Preflight по этому изменению: typecheck чист по всем 5 проектам, lint чист,
**3681 тест бота, 0 failed** (+31 новый). Оба несущих гварда подтверждены
красным до фикса: снятие проверки уверенности и раннего выхода без улик роняет
3 теста из 7.

Проверка после деплоя — на счастливом пути ничего не логируется, поэтому глазами
на `@gennetytestbot`: довести пару до свидания, дождаться T+24h (или сдвинуть
`agreedTime` в прошлое) и убедиться, что первым приходит вопрос с двумя
кнопками, а форма — только после «да». По базе:

```sh
psql "$DATABASE_URL" -c "select date_attended_a, date_attended_b, attendance_outcome_a, attendance_outcome_b, count(*) from matches group by 1,2,3,4;"
# Всё null до первого ответа. Ненулевые строки = люди отвечают.
pm2 logs gennety-bot --lines 200 --nostream | grep -E '\[attendance\]|attendance ask failed'
```

**Rollback:** откатить код и перезапустить. Колонки можно оставить — старый код
их не читает. Флага нет; чтобы отключить только вопрос, не откатывая код, такого
рычага нет by design (это шаг флоу, а не фича за тумблером).

---

**Applied 2026-08-15 (env-only) — `VENUE_SEASON_WEATHER_ENABLED=true` в проде
(PRODUCT_SPEC §3.7, DECISIONS.md).** **Ни изменения кода, ни схемы, ни Mini
App** — season/weather-код уехал ещё релизом 2026-08-02 и на дроплете
байт-в-байт совпадает с локальным (`weather.ts` + `venue-intent-v2.ts`
проверены по md5). Всё изменение — одна строка в `/opt/gennety/.env` +
`pm2 restart gennety-bot --update-env`.

Заодно из прод-`.env` удалён `CUSTOM_EMOJI_AI_SPARKLE_ID` — переменная не
читалась ни одним модулем (см. блок ниже про пять слотов), а выглядела рабочей.
Бэкап: `.env.bak.20260815-053709` (154 → 158 строк).

**Почему это вообще было выключено:** переменной **не было в `.env.example`**,
поэтому при сборке прод-окружения из шаблона она туда не попала, а дефолт —
`false` (`config.ts`: `=== "true"`). Не баг в фиче, а дыра в шаблоне; шаблон
исправлен тем же днём (все три переменные добавлены).

**Что измерено до включения, а не выведено из документации:**
- Open-Meteo с дроплета — **HTTP 200 за 0.08s**, ключ/аккаунт/квота не нужны.
- `smoke-venue-engine.ts`: живой прогноз Киева → множитель **1.1** для
  outdoor-scenic; зима 0.9 / лето 1.05 / indoor ровно 1.0.
- `venueContextMultiplier` читается **только** в `venue-intent-v2.ts`, то есть
  трогает автоназначение и не трогает платную доску смены места (§3.7b).

**Что проверено после рестарта:** прод-конфиг читает `true` и тянет живой
прогноз (21.4 °C) — проверено probe'ом на самом дроплете, а не по `.env`;
рестарты 61 → 62 без петли, `unstable restarts: 0`, **в error-логе ноль новых
строк** (8715 до и после); все кроны и peer-wait воркер зарегистрированы;
`:3100`/`:3101` слушают; `/v1/ping` ok; admin `401`.

**Наблюдаемого эффекта сегодня НЕТ, и это ожидаемо.** В проде
`venue_selection_logs` = **0 запусков за всё время**, а единственный матч со
статусом `completed` — засеянная синтетика (оба `telegramId` в бэнде
`-778000xxx`, `dispatchedAt: null`, `agreedTime` раньше `createdAt`, площадка
проставлена в строку руками). V2-селектор в проде не исполнялся ни разу, так
что первая настоящая пара станет и первой проверкой этого множителя. Смотреть
на `venueSelectionReason` (упоминает context, только если множитель реально
сдвинул победителя) и на `geoRung` в `poolSizes`.

**Dev и демо выставлены в `true` тем же днём.** `.env.local` держит weather в
паритете с продом намеренно (комментарий там это фиксирует), чтобы ранкер в
dev выбирал ту же площадку. Флипать все три или ни одного.

**Демо обновлено ТОЛЬКО через env, `pnpm demo:deploy` намеренно не
запускался** (бэкап `/opt/gennety-demo/.env.bak.20260815-192045`, оттуда же
удалён мёртвый `CUSTOM_EMOJI_AI_SPARKLE_ID`; рестарт 23 → 24, ноль новых строк
в error-логе, баннер изоляции на месте — `@gennety_demo_bot`, база
`aws-1-eu-west-1`). Причина: скрипт синкает **рабочее дерево** и делает
`db:push` в демо-базу, а в дереве в тот момент лежала незакоммиченная чужая
фича (attendance: 6 новых файлов + новые колонки в `schema.prisma`). Это ровно
та ловушка, которую фиксирует запись 2026-08-08 — демо-деплой везёт с собой всё,
что оказалось в дереве. Правка `.env` + `pm2 restart --update-env` от кода не
зависит.

**Демо здесь важнее прода**, и это стоит держать в голове: у него
`VENUE_INTENT_V2_ROLLOUT_PERCENT=100` и это **единственная площадка, где
V2-селектор вообще когда-либо исполнялся** (`venue_selection_logs` 0 → 1 на
первом же прогоне, запись 2026-08-08). То есть первую живую проверку
множителя даст демо, а не прод.

**Rollback:** `VENUE_SEASON_WEATHER_ENABLED=false` (или удалить строку) +
`pm2 restart gennety-bot --update-env`. Ни схемы, ни данных, ни кода откатывать
нечего; множитель становится константой 1.0 и запрос прогноза не делается
вовсе. Полный откат env — `.env.bak.20260815-053709`.

---

**Deployed 2026-08-15 (was PENDING) — пять env-слотов, которые ничего не включали (`75b2b46`,
DECISIONS.md).** **Нет изменения схемы, нет env, нет флагов, нет изменения Mini
App** — только `apps/bot/src/config.ts` и три dev-скрипта, так что это едет
ближайшим обычным релизом. Ни байта поведения: удалены пять ключей
(`CUSTOM_EMOJI_AI_ROUTE/VENUE/CONFIRM/CARD/SPARKLE_ID`), которые объявлялись в
конфиге и **не читались ни одним модулем** — ids давно живут в запечённом
`AI_EMOJI` (`services/ai-emoji.ts`).

Одна деталь, которую стоит знать до деплоя: **прод-переменная
`CUSTOM_EMOJI_AI_SPARKLE_ID` удалена из `.env` уже сейчас** (блок выше), то
есть `.env` и код сойдутся в момент этого релиза, а до него прод просто держит
объявленный ключ с пустым значением — как и держал.

**`CUSTOM_EMOJI_THINKING_ID` НЕ трогать:** он жив и читается `ai-stream.ts` как
последний fallback глифа на обычных продуктовых путях (вопросы Profiler,
верификация, дата-карточка). Его комментарий в конфиге утверждал обратное
(«Only consulted by explicit rich-draft demos») и исправлен тем же коммитом.

Проверки: typecheck 5/5, lint 5/5, тесты **3649 + 276 + 305** зелёные.
**Rollback:** revert коммита; в проде откатывать нечего.

---

**Deployed 2026-08-15 (was PENDING) — пустой ответ Places больше не гасит площадку на сутки
(PRODUCT_SPEC §3.7a, DECISIONS.md).** **Нет изменения схемы Prisma, нет новых
env, нет флагов, нет изменения Mini App** (`apps/webapp` не тронут) — только
бот, так что полный деплой кода несёт это целиком, плюс `pnpm demo:deploy`.
**Демо здесь важнее прода**: до этой доски в проде не дошла ещё ни одна пара (0
свиданий за всю историю), так что демо — единственное место, где путь реально
исполняется.

Пришло как «в демо опять нет фотографий на карточках смены места, старый фикс не
сработал». Фикс от 2026-08-08 в порядке и не при чём: он укрепляет **доставку
байтов**, а отказ на шаг раньше — в **резолве ссылок**. Карточка без ссылок
рисует глиф категории и запроса не делает, поэтому ретраить нечего и в логе
пусто. `lookupAndCachePhotos` кэшировал пустой массив на **сутки**, наравне с
настоящим ответом; теперь сутки заслуживает только непустой.

**Четыре вещи, которые стоит знать до рестарта:**

- **Замерено, а не выведено.** Живой демо-процесс отдавал одной киевской
  площадке 0 ссылок, свежий процесс в ту же минуту — 6, Google на 20 запросов
  подряд отвечал с фотографиями, рестарт вернул доску к 12/12. За всю историю
  демо — **ноль** строк `[venue] photo lookup … failed`, то есть запрос не
  падал, он возвращал пустоту.
- **Появляется новая строка в логе** — `[venue-change] photo lookup for <id>
  returned no photos`. Её всплеск означает системную проблему (смена field mask,
  квота, протухший `placeId`), а не «у этих мест нет фотографий». Ноль строк —
  здоровое состояние.
- **Запросов к Places становится больше ровно на безфотографийных площадках**:
  один Place Details раз в 5 минут на такую площадку, и только пока доска
  открыта (каталог тянется на открытии, а не на 4-секундном опросе состояния).
  В киевском каталоге таких почти нет, так что практически это ноль.
- **Рестарт демо сам по себе лечит симптом** (кэш в памяти) — поэтому после
  деплоя доска будет выглядеть исправной в любом случае. Настоящая проверка —
  сравнение живого процесса со свежим (ниже), а не один взгляд на доску.

Preflight по этому изменению: typecheck чист, **полный набор бота 3649 тестов /
236 файлов, 0 failed** (+1 новый). Новый тест подтверждён красным до правки: со
старым условием повторного запроса не происходит **вовсе** (1 вызов вместо 2) —
ровно те сутки пустоты.

Проверка после деплоя:

```sh
pnpm demo:deploy
ssh root@167.172.178.229 'pm2 logs gennety-demo --lines 200 --nostream | grep "returned no photos"'
# Пусто = здорово. Всплеск = смотреть на Places, а не на площадки.
```

Сквозная проверка — открыть доску смены места в демо и убедиться, что обложки
есть на всех карточках. Чтобы поймать именно регресс кэша, надо сравнить живой
процесс со свежим: если живой отдаёт меньше карточек с фотографиями, чем свежий
на той же базе в ту же минуту, дефект вернулся.

**Rollback:** откатить код и перезапустить. Откатывать больше нечего — ни схемы,
ни env, ни флага; кэш живёт в памяти и исчезает с рестартом.

---

**Deployed 2026-08-15 (was PENDING) — два claim'а на чат, которые не истекали (PRODUCT_SPEC §2.1 +
§Phase 1b, DECISIONS.md).** **Нет изменения схемы Prisma, нет новых env, нет
флагов, нет изменения Mini App** (`apps/webapp` не тронут) — только бот, так что
полный деплой кода несёт всё целиком, плюс `pnpm demo:deploy`.

Продолжение вчерашней правки порядка middleware: закрыты оба соседних дефекта,
которые та запись оставила «на решение фаундера».

- **Фото/видео-менеджер получил дедлайн** (2 ч, перевзводится на каждом
  действии с ним). До этого у `edit_photos` / `edit_video` его не было вообще, а
  состояние живёт в `bot_sessions` — то есть человек, один раз открывший «Мои
  фото» и ушедший, навсегда оставался в чате, где Profiler не пишет ответы, а
  любой текст получает в ответ «пришли фото» вместо консьержа.
- **Окно ответа Profiler'а (90 мин) перестало быть жёсткой границей.** Вопрос
  живёт 6 часов (потом stall-свип удаляет сообщение), так что 4.5 часа он был
  виден и неотвечаем. Теперь текст принимается, пока вопрос виден, — кроме
  случая, когда он сам выглядит вопросом к боту.

**Пять вещей, которые стоит знать до рестарта:**

- **На рестарте закроются все открытые сейчас медиа-менеджеры.** Дедлайн пустой
  у каждой существующей сессии, а пустой читается как истёкший (fail closed —
  то же направление, что у текстовых claim'ов 2026-08-08). Практически это
  значит: у тех, кто прямо сейчас сидит в «Моих фото», на следующем сообщении
  карточки потеряют 🗑, а менеджер закроется. Открыть заново — один тап. В
  проде это единицы аккаунтов.
- **Появляется видимое действие на входящем апдейте: ретайр карточек.** До
  N+1 вызовов `editMessageReplyMarkup` один раз на каждый брошенный менеджер
  (по числу карточек + панель). Разово, не в цикле.
- **Гейт верификации завязан на открытый менеджер.** Пользователь в редо-режиме
  «📷 загрузи другие фото» (§1.4), ушедший на два часа, вернётся к карточке
  верификации и войдёт заново одним тапом — это правильное поведение, но новое.
- **Захват ответа Profiler'а расширяется до 6 часов**, а не остаётся 90
  минутами. Риск того же класса, что существовал внутри окна (не-вопросное
  сообщение на другую тему запишется ответом), просто на более длинном
  интервале; гварда два — окно обнуляется от ЛЮБОГО другого действия
  пользователя, и вопросо-образный текст уходит консьержу. Изменение
  аддитивно: ничего, что записывалось раньше, записываться не перестало.
- **В проде окно ещё ни разу не стреляло**: из 13 отправленных вопросов все 11
  отвеченных пришли за 0–53 минуты. Так что эффект от второй половины увидят
  сначала на dev/демо.

Preflight по этому изменению: typecheck чист по всем 5 проектам, lint чист,
**полный набор бота 3645 тестов / 236 файлов, 0 failed** (+12 новых). Оба гварда
подтверждены красным до фикса: занейтраленный `mediaClaimExpired` роняет 3
теста.

Проверка после деплоя — оба пути на счастливой ветке ничего не логируют,
поэтому глазами на `@gennetytestbot`: (1) открыть «Мои фото», подождать 2 часа
(или временно уменьшить `MEDIA_CLAIM_TTL_MS` на dev), написать текст — должен
ответить консьерж, а карточки потерять 🗑; (2) дождаться вопроса Profiler'а, не
трогать бота два часа, ответить обычным текстом — должен прийти следующий вопрос
серии, а не реплика агента. Косвенно по базе:

```sh
# Доля отвеченных не должна падать; implicit-skip'ов должно стать меньше.
psql "$DATABASE_URL" -c "select skipped, count(*) from profiler_answers group by 1;"
# Сессии, застрявшие в медиа-менеджере. После деплоя они самоисцеляются на
# первом же сообщении пользователя, так что число должно только падать.
psql "$DATABASE_URL" -c "select count(*) from bot_sessions where value::jsonb->>'menuState' in ('edit_photos','edit_video');"
```

**Rollback:** откатить код и перезапустить. Откатывать больше нечего — ни схемы,
ни env, ни флага; лишнее поле `menuClaimUntil` старый код просто игнорирует.

---

**Deployed 2026-08-15 (was PENDING) — экран «кого ты хочешь видеть» перестаёт собираться на глазах
(PRODUCT_SPEC §1.3, DECISIONS.md).** **Нет изменения схемы Prisma, нет новых
env, нет флагов и НЕТ ИЗМЕНЕНИЙ СЕРВЕРА ВООБЩЕ** — диff это `apps/webapp/**`
плюс документация. То есть путь **Deploy Mini App Only**
(`./scripts/deploy-webapp.sh`); рсинкать в `/opt/gennety` нечего, **`pm2 restart`
не нужен**. Обязательно ещё `pnpm demo:deploy` — демо собирает свой бандл из того
же исходника и иначе останется на старом.

Двенадцать фотографий (~530 кБ) начинали качаться ровно в тот момент, когда
экран показывали, поэтому пользователь видел, как композиция собирается: часть
снимков уже стоит, на месте остальных тёмные плитки, последний приезжает через
полсекунды. Теперь загрузка и `decode()` стартуют, когда **начинаются экраны
профиля** — за три экрана до нужного, пока человек печатает имя.

**Четыре вещи, которые стоит знать до редеплоя:**

- **Сеть двигается, а не растёт.** Те же 12 файлов, просто раньше: замерено на
  дев-превью — на экране ИМЕНИ в сетевом логе уже все двенадцать. Лишний трафик
  возможен ровно в одном случае: аккаунт, возобновлённый ПОСЛЕ экрана
  предпочтений, — для него прогрев пропускается (`basicsStepIndex(step) <=
  basicsStepIndex("preference")`), так что и этого нет.
- **Появляется состояние, которого раньше не было: колонки без фотографий.**
  Это не дырка — две градиентные кнопки со своими подписями (проверено
  скриншотом). Видно его только тогда, когда форы не было: аккаунт,
  возобновлённый прямо на этом экране, или связь медленнее трёх экранов. В
  обычном случае раскрытие происходит через кадр-другой после монтирования,
  внутри собственного 420 мс кроссфейда сцены, то есть незаметно.
- **`PREF_REVEAL_CAP_MS` (2.6 с) — последний рубеж, а не таймер загрузки.**
  `decode()` разрешается и на ошибке, так что кэп страхует только запрос,
  который не отвечает и не падает. После него слой показывается, и то, что ещё
  летит, всплывает как раньше.
- **Чанк `onboarding` 129.35 → 130.34 kB** (+1 kB). Новых ассетов нет — те же
  двенадцать файлов.

Preflight по этому изменению: typecheck чист, lint чист, **331 тест вебаппа
зелёный** в 30 файлах (+7 новых), `vite build` чист. Главный гвард подтверждён
красным до фикса: правка «раскрывать по первому же фото» роняет 4 теста из 7.

Проверка после редеплоя — экран транзиентный и ничего не логирует, поэтому
глазами либо через дев-превью, которому не нужны ни Telegram, ни аккаунт:

```sh
./scripts/deploy-webapp.sh
pnpm demo:deploy
curl -sI https://dating-calendar.gennety.com/onboarding.html | head -1
# Дев-превью, обе темы:
#   http://localhost:5173/onboarding.html?preview=basics:preference&lang=ru&theme=dark
#   http://localhost:5173/onboarding.html?preview=basics:name&lang=ru&theme=light
# Смотреть на два момента: (1) открыть превью ИМЕНИ и убедиться в DevTools →
# Network, что 12 файлов preference/photo/* уже скачаны, ещё до экрана
# предпочтений; (2) пройти имя → возраст → пол → предпочтения и убедиться, что
# фотографии стоят с первого кадра, а не доезжают по одной.
# Холодный случай проще всего увидеть так: Network → Slow 3G + Disable cache,
# открыть превью предпочтений напрямую — колонки должны быть чистыми
# градиентами с подписями и заполниться разом, а не по одной плитке.
```

**Rollback:** передеплоить Mini App с предыдущего чекаута и оттуда же
`pnpm demo:deploy`. Откатывать больше нечего — ни схемы, ни env, ни флага, ни
серверного состояния.

---

**Deployed 2026-08-15 (was PENDING) — марка успеха: бабочка вместо галочки (PRODUCT_SPEC → Cross-Cutting
Concerns, DECISIONS.md).** Заменяет собой предыдущий PENDING-блок про «бабочка
чертит галочку» — та марка так и не уехала, так что деплоится сразу эта.
**Нет изменения схемы Prisma, нет новых env, нет флагов и НЕТ ИЗМЕНЕНИЙ СЕРВЕРА
ВООБЩЕ** — диff это `apps/webapp/**` плюс документация. Путь **Deploy Mini App
Only** (`./scripts/deploy-webapp.sh`); рсинкать в `/opt/gennety` нечего,
**`pm2 restart` не нужен**. Обязательно ещё `pnpm demo:deploy`.

Четыре разные галочки на экранах успеха (зелёный диск в verification, 84px
обводка в календаре, белый диск с **чёрной** галочкой в Type Radar, бордовый
градиентный диск в venue-change) заменены одной общей маркой, а пятый экран —
`DoneScene` онбординга — показывал `loading-orb`, спиннер загрузки на
завершённом состоянии. Теперь везде одно: **бренд-бабочка прилетает с пружиной
и остаётся.** Галочки нет ни на одном экране.

**Пять вещей, которые стоит знать до редеплоя:**

- **Галочки больше нет вообще, и это проверено, а не предположено.** Все пять
  экранов и так называют результат словами: verification передаёт `label` в саму
  марку, Type Radar, онбординг, venue-change и календарь рендерят свой заголовок
  прямо под ней. Галочка дублировала соседнее предложение. **Следствие, которое
  надо держать в голове:** марка теперь говорит «Gennety», а не «получилось» —
  шестой экран обязан иметь либо `label`, либо свой заголовок, иначе она
  превращается в украшение.
- **Два самозакрывающихся экрана закрываются на ~0.5 с раньше.**
  `SUCCESS_TOTAL_MS` 1200 → **700** (анимация проще, ей нечего дочерчивать), а
  `SUCCESS_READ_MS` намеренно не тронут — это ответ на другой вопрос. Итого
  verification и Type Radar держат экран 1800 мс вместо 2300. Если покажется
  торопливым — поднимать `SUCCESS_READ_MS`, а не `TOTAL`.
- **Хаптик переехал с 900 мс на 520 мс** — на момент, когда бабочка села.
- **Марка выросла 156 → 190px.** Проверено, что ни один из пяти экранов не
  задаёт её размеры сам (у всех только отступы и центрирование), так что
  раскладка нигде не едет.
- **Тронут `brand-butterfly.ts`, а значит и ЛОАДЕР.** Радиус градиента крыла был
  88.63 (ширина bbox) вместо 77.07 — при `r="100%"` в `objectBoundingBox` радиус
  считается от нормированной диагонали `√(w²+h²)/√2`, а не от стороны. Растяжка
  шла на 15% шире логотипа: магента расползалась по крыльям, тёмный внешний стоп
  почти не показывался. Пока бабочка была 29px это не имело значения; теперь она
  и есть марка. На лоадере разница визуально незаметна (проверено скриншотом), но
  формально он тоже меняется. Заодно поправлена вручную вставленная копия
  градиента в `verification.html` — тест на их идентичность её и поймал.

Preflight по этому изменению: typecheck чист, lint чист, **313 тестов вебаппа в
30 файлах** (0 failed), `vite build` чист. Временный стенд сравнения
(`success-variants.*` + ветка `?preview=success`) **удалён**, а не спрятан за
флаг — то же правило, что у `?v=` на экране предпочтений; в дереве и в бандле
его нет.

Проверка после редеплоя — экраны транзиентные и ничего не логируют, поэтому
глазами:

```sh
./scripts/deploy-webapp.sh
pnpm demo:deploy
curl -sI https://dating-calendar.gennety.com/verification.html | head -1
# Дев-превью, обе темы:
#   http://localhost:5173/verification.html?screen=success&lang=ru&theme=dark
#   http://localhost:5173/verification.html?screen=success&lang=ru&theme=light
# Смотреть на четыре момента:
#   (1) галочки НЕТ нигде — если она появилась, уехал старый бандл;
#   (2) правый верхний лепесток заметно тёмный (это и есть исправленный
#       градиент); если бабочка плоско-розовая — правка не доехала;
#   (3) бабочка дышит: очень слабое дыхание с периодом 3.2 с;
#   (4) вибрация приходит, когда бабочка села, а не позже.
# И лоадер (?screen=loading) обязан выглядеть как раньше.
```

**Rollback:** передеплоить Mini App с предыдущего чекаута и оттуда же
`pnpm demo:deploy`. Откатывать больше нечего — ни схемы, ни env, ни флага, ни
серверного состояния.

---

**Deployed 2026-08-15 (was PENDING) — на экране «сколько стоит найти отношения в 2026» падают деньги
(PRODUCT_SPEC §1.1, DECISIONS.md).** **Нет изменения схемы Prisma, нет новых
env, нет флагов и НЕТ ИЗМЕНЕНИЙ СЕРВЕРА ВООБЩЕ** — диff это `apps/webapp/**`
плюс документация. То есть путь **Deploy Mini App Only**
(`./scripts/deploy-webapp.sh`); рсинкать в `/opt/gennety` нечего, **`pm2 restart`
не нужен**. Обязательно ещё `pnpm demo:deploy` — демо собирает свой бандл из
того же исходника и иначе останется на старом.

Сцена 2 интро была единственной из первых трёх без визуала: сцена 0 поднимает
иконки конкурентов, сцена 1 их осыпает, а экран, который собственно задаёт
вопрос про деньги, держал строку и уходил. Теперь за вопросом в три слоя
глубины падают 14 купюр.

**Деньги начинаются вместе с ЭКРАНОМ, а не после того, как строка допечаталась**
(правка 2026-08-13 по отчёту фаундера, до первого деплоя — блок описывает уже
исправленное поведение). Первая версия вешала падение на тот же reveal-механизм,
что поднимает иконки на сцене 0: сначала ~1.4 с набора, потом холд, потом пауза —
и купюры появлялись примерно через 2 с пустого экрана. Остальные reveal'ы
передают объект, который копия только что заслужила; деньги — это погода, и она
уже идёт, когда в неё выходишь.

**Пять вещей, которые стоит знать до редеплоя:**

- **Экран удлиняется на ~0.36 с, а не на 0.9 с** (было ~3.5 с: набор 1.43 с +
  голый холд 2.04 с; стало ~3.83 с = набор + `MONEY_VIEW_MS` 2.4 с). Reveal-cue
  на этой сцене больше нет вовсе, поэтому она **короче** прежней версии на 0.56 с,
  а деньги при этом видны все ~3.8 с вместо последних 2.4. Онбординговая воронка
  здесь чувствительна — тот же расчёт, из-за которого следят за ~10.7 с
  «раздумья» Type Radar. Держится тестом (`MONEY_VIEW_MS ≤ 2800`), смотреть на
  `GET /admin/analytics/onboarding-funnel`.
- **Падение теперь ОСТАНАВЛИВАЕТСЯ через один кроссфейд после ухода со сцены.**
  Это не косметика: reveal-версия включала анимацию и никогда не выключала, то
  есть 14 анимированных купюр и три размытых слоя композились за КАЖДЫМ
  следующим экраном — включая экран метрик с его собственной анимацией убийства
  иконок. Обе границы намеренные: обрубить на смене фазы — купюры исчезнут,
  пока сцена ещё гаснет, а вся идея в том, что падение переходит под экран
  статистики.
- **Чанк `onboarding` 125.9 → 129.35 kB** (+3.4 kB, ~1 kB gzip). Новых ассетов
  нет: купюра — инлайновый SVG, всё движение — CSS-кейфреймы на композиторе.
- **Тронут `onboarding-crumble.ts`** — из него вынесен детерминированный хеш в
  новый `seeded-noise.ts` (осыпание и деньги должны сеяться одним хешем).
  Поведение не менялось, и это проверено: 11 тестов осыпания зелёные до и после.
- **Правило композиции, которое легко сломать:** купюры всегда ПОЗАДИ текста, а
  ближний слой — намеренно слабое боке. При более резкой настройке одна из его
  купюр смывала строку «найти отношения»; поймано на пошаговом кадре, не в
  тестах.

Preflight: lint чист, `vite build` чист, **287 тестов вебаппа в 28 файлах
зелёные** (мои — 23: 12 денег + 11 осыпания). Typecheck вебаппа даёт **одну**
ошибку, и она не отсюда: `butterfly-success.test.ts` соседней, ещё не
дописанной сессии (`LANDED_SCALE`) — её блок стоит выше в этом файле и уедет
тем же релизом.

**Плавность замерена, а не заявлена.** 150 кадров подряд на сцене 2 против
текстовой сцены 4 как контроля, в одной сессии headless Chromium: p50 16.7 мс,
p95 17.6 мс, худший кадр 17.7 мс, кадров дольше 20 мс — **ноль**, и цифры
совпадают с контролем до десятой. То есть падение целиком на композиторе и само
по себе не тормозит. Это десктоп, не телефон, так что это опровергает «анимация
сама по себе тяжёлая», но не заменяет проверку на устройстве.

Проверка после редеплоя — экран транзиентный и ничего не логирует, поэтому
глазами. Продовый онбординг малотрафиковый, так что смотреть на dev-превью,
которому не нужны ни Telegram, ни аккаунт:

```sh
./scripts/deploy-webapp.sh
pnpm demo:deploy
curl -sI https://dating-calendar.gennety.com/onboarding.html | head -1
# Дев-превью, обе темы:
#   http://localhost:5173/onboarding.html?preview=intro:2&lang=ru&theme=dark
#   http://localhost:5173/onboarding.html?preview=intro:2&lang=ru&theme=light
# Смотреть на три момента: (1) купюры уже падают, ПОКА строка ещё печатается —
# если экран секунду пустой, значит падение опять повесили на reveal;
# (2) кадр заполнен сверху донизу с первого мгновения, а не наполняется сверху
# занавесом; (3) вопрос читается целиком — ни одна купюра не смывает строку.
# И что падение выключается: через ~5 с после входа на сцену (то есть ~1 с
# после ухода с неё) вот это обязано вернуть 0 —
#   document.getAnimations().filter(a=>a.animationName?.startsWith('money')).length
# Анимация быстрая, одним скриншотом её не поймать. Заморозить и шагать:
#   document.getAnimations().forEach(a=>a.pause())
#   document.getAnimations().forEach(a=>a.currentTime=(a.currentTime||0)+900)
```

**Rollback:** передеплоить Mini App с предыдущего чекаута и оттуда же
`pnpm demo:deploy`. Откатывать больше нечего — ни схемы, ни env, ни флага, ни
серверного состояния.

---

**Deployed 2026-08-15 (was PENDING) — календарь после тикет-гейта приходит новым сообщением, а не
невидимой правкой (PRODUCT_SPEC §3.5b / §3.6, DECISIONS.md).** **Нет изменения
схемы Prisma, нет новых env, нет флагов, нет изменения Mini App**
(`apps/webapp` не тронут) — только бот, так что полный деплой кода несёт всё
целиком, плюс `pnpm demo:deploy`.

Пришло как «демо зависает на гейте: используем билет, получаем "билет готов,
ждём вторую сторону" — и всё». Партнёр подтверждал: в демо-базе
`ticketStatus = completed`, оба слота оплачены с разницей в 15 секунд, 84 слота
сетки записаны — и **ноль** событий чата после второй оплаты. `startScheduling`
правил `editMessageText`'ом отслеживаемую post-accept карточку, которая к тому
моменту была третьей снизу (под ней уже лежали карточка билета и уведомление об
оплате). Правка не даёт уведомления → кнопка календаря появлялась выше, и её
никто не видел.

**Четыре вещи, которые стоит знать до рестарта:**

- **Это НЕ демо-баг.** Код общий, `TICKET_FEATURE_ENABLED=true` в проде, и путь
  мужчины, оплатившего «за обоих», ломался так же. В проде не выстрелило только
  потому, что до гейта там не дошла ещё ни одна пара (0 свиданий за всю
  историю) — то есть проверять надо в демо или на `@gennetytestbot`, в проде
  сейчас исполнить этот путь нечем.
- **Меняется видимое поведение: старая карточка «Прийнято ✨ Чекаємо на іншу
  сторону» теперь УДАЛЯЕТСЯ**, а календарь приходит свежим сообщением внизу
  чата (с уведомлением). Тот же приём, что у контрпредложения по календарю,
  который так работал всегда. Удаление best-effort: Telegram разрешает боту
  удалять своё сообщение 48 ч, отказ проглатывается и календарь всё равно
  уходит.
- **Затрагивает все три хендовера гейта** — завершение, бесплатный календарь по
  истечении окна и после возврата, — плюс отложенный календарь «покрытой»
  стороны (у неё карточка была ещё глубже: под карточкой билета И под DM «он
  оплатил твой билет ❤️»).
- **Путь без гейта не тронут** и по-прежнему правит расписку на месте — там она
  реально последнее сообщение, и это тот самый «morph», о котором говорит §3.4.
  Тест держит обе стороны.

Preflight по этому изменению: typecheck чист, **полный набор бота 3633 теста /
236 файлов, 0 failed** (+2 новых). Новый тест подтверждён красным до фикса
(вызывался `editMessageText` вместо `deleteMessage` + `sendMessage`).

Проверка после деплоя — на счастливом пути ничего не логируется, поэтому глазами
в демо-боте: дойти до гейта, нажать «использовать билет» и убедиться, что через
~15 секунд **внизу чата** появляется карточка календаря. Косвенно, по базе:

```sh
# После завершённого гейта в чате обязано быть событие ПОСЛЕ второй оплаты.
# Ноль событий при ticket_status='completed' = баг вернулся.
psql "$DATABASE_URL" -c "select id, ticket_status, calendar_message_id_a, calendar_message_id_b from matches where ticket_status='completed' order by updated_at desc limit 5;"
```

**Rollback:** откатить код и перезапустить. Откатывать больше нечего — ни схемы,
ни env, ни флага; матчи, уже прошедшие гейт, свою карточку календаря сохраняют.

---

**Deployed 2026-08-12 — релиз из 7 runtime-коммитов: шесть PENDING-блоков разом
(`bcb82c9`).** Полный деплой кода + Mini App + демо. **Изменения схемы Prisma
нет** (`db:drift-check` **OK**, пушить было нечего), **нет изменений env, нет
флипов флагов**. Прод поднят с `677e4e2` до `bcb82c9`.

Порядок был: Deploy Full Server Code → `pnpm db:drift-check` → `pm2 restart` →
`./scripts/deploy-webapp.sh` → `pnpm demo:deploy`. Mini App понадобился из-за
`58bc3e3` (осыпающиеся иконки конкурентов) — три остальных бот-коммита его не
трогают, но релиз един.

**Одно наблюдение, стоящее записи: заявка была на одну правку, а прод отставал
на семь коммитов.** Блок про `protect_content` честно писал «только бот, нет
изменений Mini App» — это верно про ТУ правку и неверно про релиз, который её
понёс. Считайте диапазон (`git log 677e4e2..HEAD`) перед деплоем, а не читайте
шапку верхнего блока: она описывает свой коммит, а не то, что реально поедет.

**Проверено на живом проде (замерено, а не выведено):**

- **Главное для этого релиза — защита фото партнёра.** Значение константы
  прочитано из ОБОИХ рантаймов настоящим импортом модуля, а не выведено из
  `.env`: прод → `DEMO_MODE_ENABLED = false`, `PROTECT_PARTNER_MEDIA = **true**`
  (защита на месте, поведение прежнее); демо → `true` / **`false`** (снята).
  Все 11 вызовов на шести поверхностях читают константу; единственные
  оставшиеся `protect_content: true` — в generic `profile-media-dispatch.ts`,
  который берёт флаг у вызывающего и обслуживает в том числе просмотр
  пользователем СВОЕГО профиля.
- **Прод байт-в-байт равен HEAD**: md5-свип 771 файла дал 767 совпадений и 4
  расхождения — все под `apps/bot/tmp/`, которые rsync исключает по дизайну
  (ровно как в релизе 2026-08-07).
- `Bot @gennetybot started`, все 16 кронов + `[worker] Peer-wait shimmer every
  20000ms`, ежедневный `Drop matching "0 18 * * *"`, `:3100`/`:3101` слушают.
- Рестарт 60 → **61** (один инкремент), `unstable restarts: 0`, и **в
  error-лог с момента рестарта не записано ни строки** — файл последний раз
  менялся в 15:01 UTC при рестарте в 19:18 UTC. Две записи `peer-decided nudge
  failed: chat not found` там исторические.
- `/v1/ping` ok, admin `401`, **все 11 страниц Mini App 200**,
  `supportedCities` по-прежнему только Киев, `features` без изменений.
- Демо переехало тем же исходником: баннер называет `@gennety_demo_bot` и базу
  `aws-1-eu-west-1` (у прода `aws-0-`), оба демо-подавления кронов на месте,
  рестарт 19 → 20.

rsync dry-run показал ровно **2** удаления, оба — устаревшие артефакты
`apps/video/build`. Оба бэкапа БД, оба ключа `keys/*.p8` и все 15 снапшотов
`.env.bak.*` проверены на месте ПОСЛЕ синка.

**Не проверено, и сознательно:** запись экрана в демо-боте — это надо делать
глазами с телефона. В проде проверять нечего: там это no-op по построению, и
константа прочитана.

**Rollback:** пересинкать чекаут на `677e4e2`, перезапустить, оттуда же
редеплоить Mini App и демо. Откатывать больше нечего — ни схемы, ни env, ни
флага.

---

**Deployed 2026-08-12 (was PENDING) — фото партнёра перестают чернеть на записи
экрана в демо (PRODUCT_SPEC §3.7a, DEMO_MODE.md, DECISIONS.md).** Задеплоено
2026-08-12 в релизе из 7 коммитов (блок наверху файла). **Нет
изменения схемы Prisma, нет новых env, нет флагов, нет изменения Mini App**
(`apps/webapp` не тронут) — только бот, так что полный деплой кода несёт всё
целиком, плюс `pnpm demo:deploy`.

`protect_content` снят со всех шести поверхностей, где видно лицо партнёра, —
**но только когда процесс запущен с `DEMO_MODE_ENABLED`**. Клиенты Telegram
затирают защищённое медиа на скриншоте и при записи экрана, поэтому демо,
которое снимают для инвестора, писало чёрный прямоугольник вместо партнёра.

**Три вещи, которые стоит знать до рестарта:**

- **В проде не меняется ничего, и это проверяемо, а не заявлено.**
  `PROTECT_PARTNER_MEDIA = !DEMO_MODE_ENABLED`, а `DEMO_MODE_ENABLED` в
  `/opt/gennety/.env` не выставлен (и не может быть — `assertDemoIsolation`
  уронит процесс на старте). Тесты самих отправителей продолжают фиксировать
  `protect_content: true`; новый тест в `demo/config.test.ts` подтверждён
  красным при захардкоженном `true`.
- **Проверять надо на демо, а не на проде.** В проде это no-op по построению;
  единственный способ увидеть эффект — снять экран в демо-боте.
- **Замыленная share-копия дата-карточки не тронута** — она и раньше уходила
  без защиты в обоих режимах, и её тест это держит.

Preflight по этому изменению: typecheck чист, lint чист по всем 5 проектам,
**полный набор бота 3631 тест / 236 файлов, 0 failed** (+2 новых).

Проверка после деплоя — путь ничего не логирует, поэтому глазами в демо-боте:
дойти до питча (или открыть «Моё свидание» на назначенном свидании), включить
запись экрана и убедиться, что фотографии видны. На проде убедиться в обратном —
что защита на месте:

```sh
pnpm demo:deploy
ssh root@167.172.178.229 'grep -c "^DEMO_MODE_ENABLED" /opt/gennety/.env'
# 0 = в проде флага нет, значит PROTECT_PARTNER_MEDIA=true и поведение прежнее.
ssh root@167.172.178.229 'pm2 logs gennety-demo --lines 40 --nostream' | head -12
```

**Rollback:** откатить код и перезапустить. Откатывать больше нечего — ни схемы,
ни env, ни флага, ни состояния.

---

**Deployed 2026-08-12 (was PENDING) — иконки конкурентов держатся на втором
интро-экране и осыпаются там (PRODUCT_SPEC §1.1, DECISIONS.md).** Задеплоено
2026-08-12 в релизе из 7 коммитов. **Нет изменения схемы
Prisma, нет новых env, нет флагов и НЕТ ИЗМЕНЕНИЙ СЕРВЕРА ВООБЩЕ** — диff это
`apps/webapp/**` плюс документация. То есть путь **Deploy Mini App Only**
(`./scripts/deploy-webapp.sh`); рсинкать в `/opt/gennety` нечего, **`pm2 restart`
не нужен**. Обязательно ещё `pnpm demo:deploy` — демо собирает свой бандл из того
же исходника и иначе останется на старом.

**Три вещи, которые стоит знать до редеплоя:**

- **Второй интро-экран держится примерно на 0.4с дольше** (было: 1.5с холд
  строки; стало: 250мс пауза → 1635мс осыпание). Онбординговая воронка здесь
  чувствительна — тот же расчёт, из-за которого следят за ~10.7с «раздумьем»
  Type Radar. Длительность выводится из самих плиток (`CRUMBLE_TOTAL_MS`), а не
  задана числом, поэтому перетюнить осыпание = холд поедет вместе с ним; тест
  держит её под двумя секундами.
- **Чанк `onboarding` вырос 124.5 → 125.9 kB** (+1.4 kB, ~0.4 kB gzip). Никаких
  новых ассетов: плитки режутся из тех же трёх PNG, которые экран уже грузил.
- **Появился dev-only маршрут `?preview=intro[:n]`** — за `import.meta.env.DEV`,
  то есть в продакшн-бандле его нет вовсе, как и у `?preview=basics`.

Проверка после редеплоя — экран транзиентный и ничего не логирует, поэтому
глазами. Продовый онбординг малотрафиковый, так что смотреть на `@gennetytestbot`
(нужен HTTPS-туннель и `WEBAPP_URL` на него) либо на дев-превью, которому не
нужны ни Telegram, ни аккаунт:

```sh
./scripts/deploy-webapp.sh
pnpm demo:deploy
curl -sI https://dating-calendar.gennety.com/onboarding.html | head -1
# Дев-превью, обе темы:
#   http://localhost:5173/onboarding.html?preview=intro&lang=ru&theme=dark
#   http://localhost:5173/onboarding.html?preview=intro:1&lang=ru&theme=light
# Смотреть надо на два момента: (1) на переходе с первого экрана на второй
# иконки не должны шелохнуться, (2) до начала падения по иконке не должно быть
# сетки волосяных швов — именно это чинил переход на абсолютные px.
```

**Rollback:** передеплоить Mini App с предыдущего чекаута и оттуда же
`pnpm demo:deploy`. Откатывать больше нечего — ни схемы, ни env, ни флага, ни
серверного состояния.

---

**Deployed 2026-08-12 (was PENDING) — письма-возвраты называют тот шаг, на
котором человек реально встал (PRODUCT_SPEC §1.5, DECISIONS.md).** Задеплоено
2026-08-12 в релизе из 7 коммитов. **Нет изменения схемы
Prisma, нет новых env, нет флагов, нет изменения Mini App** (`apps/webapp` не
тронут) — только бот, так что полный деплой кода несёт всё целиком, плюс
`pnpm demo:deploy`.

`User.onboardingStep` не может ответить, где человек остановился: у колонки
четыре значения, и **весь** входной мини-апп схлопывается в одно из них
(`/consent` и `/language` оба пишут `language`, дальше она не двигается до
`/complete`). Воркер подставлял её описание в промпт, поэтому любой, кто бросил
регистрацию внутри мини-аппа, получал письмо «осталось выбрать язык». Теперь
этап выводится из состояния (`services/onboarding-stage.ts`) — серверный
двойник клиентского `postVisualPhaseFromRemote`.

**Четыре вещи, которые стоит знать до рестарта:**

- **Сегодня в проде это никому ничего не пришлёт, и так и должно быть.**
  Замерено 2026-08-12: 15 незавершённых онбордингов, у **всех** пятнадцати
  `reEngagementNextAt = null` — у 10 цепочка вообще не вооружалась, у 5
  исчерпана. То есть фикс вступает в силу для следующего, кто бросит
  регистрацию, а не задним числом. Не ждите активности в логах сразу после
  рестарта.
- **Один живой пострадавший в проде уже был**: аккаунт с `language = uk` и
  принятыми условиями получил все пять касаний про выбор языка. Он и стал
  фикстурой регрессионного теста.
- **Запрос воркера получает два джойна** (`profile`, `onboarding_progress`) на
  батч ≤50 строк раз в 5 минут. Обе связи 1:1 по индексированному ключу.
- **Текст меняется у всех, кто попадёт в цепочку**, включая fallback: тому, кто
  ещё не прошёл регистрацию, он больше не говорит «профиль почти готов» (у него
  нет профиля вовсе). Новых ключей i18n нет — эти строки всегда жили в самом
  воркере.

Preflight по этому изменению: typecheck чист, lint чист по всем 5 проектам,
**3629 тестов бота** (236 файлов, 0 failed; +21 новый). Регрессионный тест
подтверждён красным до фикса (промпт содержал «language picker» для аккаунта,
который язык уже выбрал). `apps/webapp` этим изменением не тронут — в дереве
параллельно шла чужая работа по онбордингу, она в этот коммит не входит.

Проверка после деплоя — на счастливом пути воркер не логирует ничего, поэтому
проверяем присутствие модуля и отсутствие ошибок:

```sh
ssh root@167.172.178.229 'ls -l /opt/gennety/apps/bot/src/services/onboarding-stage.ts'
pm2 logs gennety-bot --lines 200 --nostream | grep 'Re-engagement'
# Пусто = ничего не падает. Сквозная проверка — на @gennetytestbot: начать
# онбординг, выбрать язык, принять условия, закрыть мини-апп и подождать 15 мин;
# в письме не должно быть ни слова про выбор языка.
```

**Rollback:** откатить код и перезапустить. Больше нечего — ни схемы, ни env, ни
флага, ни состояния.

---

**Deployed 2026-08-12 (was PENDING) — брифинг безопасности доезжает до
приложения, и два уведомления получают право пробивать Focus (PRODUCT_SPEC
§Phase 4, ARCHITECTURE → APNs, DECISIONS.md ×2).** Задеплоено 2026-08-12 в
релизе из 7 коммитов. **Нет изменения схемы Prisma, нет новых env,
нет флагов, нет изменения Mini App** (`apps/webapp` не тронут) — только бот,
так что полный деплой кода несёт всё целиком, плюс `pnpm demo:deploy`.
Клиентская половина уезжает из iOS-репо отдельно.

**Едет вместе с серверной половиной 5.3 (дроп-пуш), которая ЕЩЁ НЕ
ЗАДЕПЛОЕНА** — см. её блок ниже. Оба меняют `buildAlertPayload`, оба bot-only,
оба без схемы и env: это один рестарт, а не два. Порядок между ними не важен,
но разделять их незачем.

**Четыре вещи, которые стоит знать до рестарта:**

- **Появляется пуш, которого не существовало** — брифинг безопасности T-1.5ч
  женщине с `platform ∈ {mobile, both}`. В проде сегодня это ноль отправок: ни
  одна пара ещё не дошла до `scheduled` (0 свиданий за всю историю), так что
  первое реальное срабатывание будет не на этом деплое.
- **Меняется, КОМУ уходит DM брифинга.** Фильтр `telegramId > 0n` заменён на
  `telegramReachable`, поэтому аккаунт, вошедший через Telegram, но никогда не
  нажимавший Start, перестаёт получать DM — он его и не видел, просто рельса
  отчитывалась успехом. Женщина на `both` теперь получает обе рельсы.
- **`buildAlertPayload` начинает ставить `interruption-level: time-sensitive`**
  — но только двум типам (`safety.brief`, `proxy.opened`). Для остальных ~25
  вызывающих payload байт-в-байт прежний, и тест держит именно ОТСУТСТВИЕ
  уровня у `match.proposed` / `proxy.message` / `feedback.due`.
- **10 новых ключей i18n** (`safetyBriefPushTitle`/`Body` × 5 языков),
  аддитивно. Копия не называет ни партнёра, ни место — причина в DECISIONS.

Preflight: typecheck чист по всем 5 проектам, **4139 тестов** (бот 3608 /
shared 276 / webapp 255, 0 failed; +10 новых). Полный прогон, не выборочный —
`matching.test.ts` (126 тестов) зелёный, то есть регрессия, которую 5.3 внёс
подменой prisma по порядку вызовов, не повторилась. Гейт на уровень прерывания
подтверждён красным до фикса (2 падения).

Проверка после деплоя — на счастливом пути ничего не логируется, поэтому
смотрим отсутствие ошибок; сквозная проверка требует пары на `scheduled`, чего
в проде пока не бывает, так что она делается на `@gennetytestbot` или в демо:

```sh
pm2 logs gennety-bot --lines 200 --nostream | grep '\[pre-date-safety\]'
# Пусто = ничего не падает.
psql "$DATABASE_URL" -c "select count(*) filter (where safety_note_sent_at is not null) briefed, count(*) from matches where status='scheduled';"
```

**Пробой через Focus проверяется ТОЛЬКО на устройстве.** `simctl push` не будит
NSE и не эмулирует Focus, а сам уровень невидим, пока Focus не включён, — то
есть на симуляторе его нельзя отличить от обычного уведомления ни при каком
раскладе. Проверено то, что проверяемо: payload юнит-тестами, а клиентская
предпосылка — дифференциальным зондом (`timeSensitiveSetting` = `notSupported`
без entitlement и `enabled` с ним, при одинаковой авторизации).

**Rollback:** откатить код и перезапустить. Больше нечего — ни схемы, ни env,
ни флага; новые ключи i18n становятся мёртвым весом, как только вызывающий
исчез.

---

**Deployed 2026-08-12 (was PENDING) — брошенный редактор меню больше не съедает
ответ Profiler'у (PRODUCT_SPEC §Phase 1b, DECISIONS.md).** Задеплоено
2026-08-12 в релизе из 7 коммитов. **Нет изменения схемы
Prisma, нет новых env, нет флагов, нет изменения Mini App** (`apps/webapp` не
тронут) — только бот, так что полный деплой кода несёт всё целиком, плюс
`pnpm demo:deploy` после него.

Сброс протухшего menu-claim переехал из menu-роутера в раннюю middleware
`bot.ts`, рядом с уже существующим сбросом match-flow claim. Причина в порядке:
menu-роутер смонтирован ПОСЛЕ profiler-роутера, а тот читает `menuState` в своей
проверке `idle` — поэтому брошенный несколько часов назад `edit_bio` был ещё
выставлен, когда Profiler смотрел: он отказывался записать ответ, **закрывал
answer window** (после чего живой вопрос уже не ответить обычным текстом), и
только потом menu-роутер сбрасывал claim и отдавал текст консьержу.

**Три вещи, которые стоит знать до рестарта:**

- **Поведение самих редакторов меню не меняется** — те же TTL (30/60 мин), тот
  же мягкий отказ (сообщение уходит агенту, тот вернёт редактор). Меняется
  только МОМЕНТ сброса: на одну middleware раньше, до всех роутеров. Три теста в
  `menu.test.ts`, которые дёргали menu-роутер изолированно, теперь повторяют этот
  ранний шаг — как делает реальная цепочка.
- **Одно микро-отличие, осознанное:** сброс теперь стоит до `voiceHandler`, а тот
  подставляет транскрипт в `ctx.message.text`. Значит голосовое, чей транскрипт
  начинается с «/», больше не считается командой, закрывающей claim. Whisper
  такое практически не выдаёт; усложнять ради этого не стал.
- **В проде это ещё ни разу не стреляло** — в `chat_events` за всю историю нет
  ни одного случая «вопрос Profiler'а → текст → ответ агента» (13 вопросов, все
  11 отвеченных пришли за 0–53 мин). Так что регрессии ждать неоткуда, но и
  подтверждения в логах после деплоя не будет.

Preflight этого изменения: typecheck чист по всем 5 проектам, lint чист,
**3597 тестов бота** (0 failed, +2 новых). Тест порядка подтверждён красным до
фикса (`menuState` оставался `edit_bio`, ответ не записывался).

Post-deploy проверка — путь ничего не логирует на счастливой ветке, поэтому
проверяется на `@gennetytestbot`: дождаться батча Profiler'а, тапнуть «Обо мне»
в меню и НЕ отвечать, подождать 30+ минут, затем ответить на висящий вопрос
Profiler'а обычным текстом — должен прийти следующий вопрос серии, а не реплика
агента. Косвенно:

```sh
psql "$DATABASE_URL" -c "select count(*) filter (where answer_text is not null) answered, count(*) filter (where skipped and answer_text is null) skipped from profiler_answers;"
# Доля answered должна расти, а не оставаться на месте.
```

**Rollback:** откатить код и перезапустить. Откатывать больше нечего — ни схемы,
ни env, ни флага, ни состояния.

---

**Deployed 2026-08-12 (was PENDING) — дроп-пуш для приложения, с блюр-картинкой
(PRODUCT_SPEC §3.3, ARCHITECTURE → APNs, DECISIONS.md ×3).** Задеплоено
2026-08-12 в релизе из 7 коммитов. **Нет изменения схемы
Prisma, нет новых env, нет флагов, нет изменения Mini App** (`apps/webapp` не
тронут) — только бот, так что полный деплой кода несёт всё целиком, плюс
`pnpm demo:deploy`. Клиентская половина уезжает из iOS-репо отдельно.

Задача звучала как «добавить картинку к уведомлению о дропе», а уведомления не
было вовсе: ни одного `sendPushToUser` на пути питча, при том что `pitch.ts`
шесть недель носит комментарий «their pitch goes via the push path». Восьмой раз
та же дыра, что у тикет-гейта, календаря и прокси-чата.

**Четыре вещи, которые стоит знать до рестарта:**

- **Появляется новый пуш, которого раньше не существовало** — один на каждого
  участника с `platform ∈ {mobile, both}` на каждый диспатч питча. В проде такой
  человек ровно один (аккаунт фаундера на мобильной рельсе), а дроп идёт теперь
  ежевечерне (`DROP_CADENCE=daily`), так что это не «редкое событие»: оно
  ежедневное. Синтетические профили не в счёт — у них нет `pushToken`.
- **`buildAlertPayload` начинает ставить `mutable-content: 1`** — но только на
  пуше с `data.image`, а такой пока ровно один. Для всех остальных ~25 вызывающих
  payload байт-в-байт прежний.
- **Подписанная ссылка на фото партнёра живёт 24 часа вместо 10 минут** (только
  в пуше; экран питча не тронут). Это не ослабление: маршрут байтов
  перепроверяет право на каждый запрос, а сам матч умирает на дедлайне решения —
  см. DECISIONS.md.
- **Копия пуша обязана совпадать с макетом на пре-пермишн экране iOS**
  (`prenote.mock.*`). 10 новых ключей i18n (`matchDropPushTitle`/`Body` × 5
  языков), аддитивно.

Preflight по этому изменению: typecheck чист по всем 5 проектам, lint чист,
**4126 тестов** (бот 3595 / shared 276 / webapp 255, 0 failed; +11 новых).
Регрессия, которую поймал полный набор и которой не было бы при выборочном
прогоне: `matching.test.ts` подменяет `prisma.match.findUnique` **по порядку
вызовов**, так что второй запрос внутри `sendMatchProposal` ломал ретрай-тест.
Из-за этого сервис принимает уже загруженную строку, а не `matchId` — что заодно
убирает лишний запрос с горячего пути диспатча.

Проверка после деплоя — на счастливом пути ничего не логируется, так что
смотрим отсутствие ошибок и сам факт отправки:

```sh
pm2 logs gennety-bot --lines 200 --nostream | grep -E '\[match-drop-push\]|\[pitch\] drop push'
# Пусто = ничего не падает. Затем, после ближайшего вечернего дропа:
psql "$DATABASE_URL" -c "select platform, count(*) from users where push_token is not null group by 1;"
```

Сквозную проверку «уведомление с замыленным фото на экране» сделать нечем без
живого устройства: `simctl push` НЕ будит Notification Service Extension
(проверено — в логе симулятора доставка есть, запуска `com.gennety.ios.notifications`
нет). Клиентская сторона проверена прогоном точки входа расширения в процессе;
подробности в iOS-репо, блок 5.3.

**Rollback:** откатить код и перезапустить. Больше нечего — ни схемы, ни env, ни
флага; новые ключи i18n становятся мёртвым весом, как только вызывающий исчез.

---

**Deployed 2026-08-11 — «% платящих»: раздел монетизации (PRODUCT_SPEC не
затронут, ARCHITECTURE → Admin API + Purchase ownership, DECISIONS.md ×2).**
**Нет изменения схемы Prisma, нет новых env, нет флагов** — всё выводится из уже
существующих таблиц, так что `db:push` НЕ понадобился (`db:drift-check` **OK**).
Прод поднят с `68757f7` до `677e4e2`. Последовательность была: Deploy Full
Server Code → `pnpm db:drift-check` → `pm2 restart` → `pnpm demo:deploy`;
дашборд уехал раньше сам (автодеплой на Vercel по пушу).

**Порядок важен: сервер первым.** Вкладка читает новый
`GET /admin/analytics/monetization`; фронт вызывает его через `.catch(() => null)`
и при отсутствии эндпоинта просто не рисует вкладку, а не роняет весь дашборд.
Обратный порядок дал бы пустую вкладку на живом дашборде.

Что появляется: `GET /admin/analytics/monetization` (кэш 15 мин, `?fresh=1`,
заголовки `X-Data-Generated-At` / `X-Data-Cache` — как у остальных
аналитических вкладок), новая вкладка **Monetization** в дашборде и блок
`monetization` внутри `GET /admin/analytics/founder-digest`, который Hermes
дёргает каждый прогон.

**Четыре вещи, которые стоит знать до рестарта:**

- **Знаменатель — `registeredReal`, и он МЕНЬШЕ `users.total`.** Тестовые и
  синтетические аккаунты исключены из числителя, знаменателя и выручки (тот же
  вердикт `user-health.ts`, что и у воронки). Сегодня в проде это **19 против
  50** — 30 синтетических профилей плюс фаундерский аккаунт. `registeredReal:
  50` после деплоя означало бы, что исключение не сработало; это главный
  признак поломки.
- **Эндпоинт делает два скана `users` за запрос** (свой + внутри
  `classifyAllUsers`) плюс три вызова индекса покупок с фильтрами по датам.
  Осознанный размен ради того, чтобы не копировать правила классификации;
  прикрыт кэшем на 15 минут. На 50 аккаунтах это миллисекунды — пересмотреть,
  когда база уйдёт в пятизначные числа.
- **`founder-digest` стал тяжелее на ту же работу.** Он тоже кэшируется на 15
  минут и дёргается раз в неделю, так что цена номинальна, но если он начнёт
  таймаутить — причина здесь.
- **Ничего это пока не показывает, кроме нулей**, и это правильный результат:
  0 покупок и 0 свиданий за всё время. Сквозная проверка «метрика двигается»
  делается на `@gennetytestbot` мок-рельсом магазина билетов, а не на проде.

Preflight: typecheck чистый по всем 5 проектам, lint чистый, **4113 тестов**
(бот 3582 / 234 файла, shared 276 / 15, webapp 255 / 25 — 0 failed),
`pnpm build` чистый, `security:secrets` чистый (1049 файлов),
**`security:audit` 0 advisories** (оверрайды с 2026-08-07 не протухли),
`openapi:lint` valid (9 warnings — базовая линия). rsync dry-run показал ровно
**2** удаления, оба — устаревшие артефакты `apps/video/build`. Оба бэкапа БД,
оба ключа `keys/*.p8` и все 15 снапшотов `.env.bak.*` на месте после синка.

**Проверено на живом проде (замерено, а не выведено):**

- **`registeredReal: 19`, а не 50** — главный признак, что исключение тестовых
  сработало. Рядом `excludedTestUsers: 31` (30 синтетиков + фаундер),
  `ofActivated.base: 4`, `ofPaywallReached: {base: 0, pct: null}` — знаменатель
  пуст, потому что ни одна пара не дошла до гейта билетов.
- **Нули там, где их ждали, и `null` там, где данных нет**: `payers: 0`,
  `revenue.allTimeUsdCents: 0`, `byKind: []`, а `arppuUsdCents` / `growthPct` /
  `repeatRatePct` / `medianDaysToFirstPayment` — `null`. Это то самое правило
  «нет данных ≠ 0%».
- **Разрезы уже показывают что-то настоящее.** `byChannel` разделил 19 реальных
  на `organic: 15` и **`promo:DATE_WITH: 4`** — то есть промо-кампания уже
  привела людей; `byGender` даёт `unknown: 12` (столько не дошли до вопроса о
  поле), `byCity` — Киев 6 / Харьков 1, `byTrack` — legacy 12 / general 7.
  Когорт 4, две свежие помечены `censored: true` по 14-дневному порогу.
- **Кэш ведёт себя как у остальных вкладок**: первый запрос `x-data-cache: miss`,
  второй `hit`, `?fresh=1` снова `miss`, `X-Data-Generated-At` отдаётся; без
  ключа — `401`.
- **`founder-digest` несёт блок `monetization`** — Hermes подхватит его в
  ближайшем прогоне без изменения его крона.
- Рестарт 59 → **60**, `unstable restarts: 0`, **ноль ошибок от нового PID**
  (последняя запись в error-логе — 2026-08-08, за двое суток до деплоя). Все
  кроны зарегистрированы, включая ежедневный `Drop matching "0 18 * * *"` и
  `Synthetic test partner`. `/v1/ping` ok, admin `401`, **все 11 страниц Mini
  App 200**, `supportedCities` по-прежнему только Киев.
- Демо переехало тем же исходником: баннер называет `@gennety_demo_bot` и базу
  `aws-1-eu-west-1` (у прода `aws-0-`), оба демо-подавления кронов на месте,
  рестарт 18 → 19, `demo-api` ping ok.

**Одна деталь про синхронность деревьев.** Пока шёл деплой, параллельная сессия
закоммитила `677e4e2` — правку описания в `openapi/gennety-v1.yaml`. Файл
рантаймом бота не читается вовсе (ноль ссылок в `apps/bot/src` и `packages`;
это контракт для генератора iOS и для `openapi:lint`), поэтому он был донесён
отдельным `rsync openapi/` **без рестарта**. Полный md5-свип 765 файлов
подтвердил: прод байт-в-байт равен `677e4e2`.

**Rollback:** откатить код и перезапустить, редеплоить дашборд с предыдущего
чекаута. Откатывать больше нечего — ни схемы, ни env, ни флага, ни состояния.

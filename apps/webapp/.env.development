# Loaded by Vite for `vite dev` / `vite build --mode development`.
# For the Mini App to actually reach the local bot you'll need a cloudflared
# tunnel pointing at :3101 — paste its https URL here.
VITE_API_BASE_URL=
